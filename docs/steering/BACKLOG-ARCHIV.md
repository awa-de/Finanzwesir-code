# BACKLOG-ARCHIV – Finanzwesir 2.0
Stand: 2026-07-12 | Session: RITUAL-OPT-2 | Geändert von: Claude

Abgeschlossene Items (append-only, **neueste unten**). Jede Zeile ist datiert (Spalte „Abgeschlossen") — die physische Reihenfolge ist für die Forensik unerheblich, es wird ausschließlich ans Ende angehängt (read-frei über `tools/append-log-line.py`, nie per Voll-Read).
Rotation: per `tools/rotate-table-log.py`, Auswahl nach Datum (nicht Position), Überhang wandert nach Jahr gruppiert in `BACKLOG-ARCHIV-YYYY.md`. Erstrotation am 2026-07-12 (RITUAL-OPT-2 Punkt 9): kompletter Altbestand vor diesem Datum (207 Zeilen, 2026-05-04 bis 2026-07-07, war zuvor teils in umgekehrter Reihenfolge) liegt jetzt in `BACKLOG-ARCHIV-2026.md`. Dieses aktive File enthält seither ausschließlich Zeilen ab 2026-07-06 in tatsächlich chronologischer Reihenfolge.

| ID | Bereich | Titel | Abgeschlossen | Session |
|----|---------|-------|---------------|---------|
| AP-tailwind-01 → Fable-Baukasten-Freigabe | Design/App-Fabrik | **Meilenstein: Tailwind-Design-System-Fundament etabliert.** `AP-tailwind-01` (Befund/Forschung, GELB — 1 bewusst benannter Research-Gap: Ghost-`.hbs`-Kette repo-intern nicht verifizierbar, kein neuer Fund) fand: Tailwind war als Ziel-Fundament kanonisch entschieden (`CI-POOL-ROLLENKONTRAKT.md`), aber im App-Pool faktisch noch nicht angekommen — `prokrastinations-preis` (einzige produktive App) enthielt 0 Tailwind-Utility-Klassen, 3 Standalone-Prototypen mit CI-fremdem Design-System, 13/15 Engine-Testseiten mit hartem Legacy-Border. Befund: 25 UI-Primitiven, 9 Findings, 6 Forschungsthesen (offizielle Tailwind-/Headless-UI-/Refactoring-UI-Recherche), 16 offene Fable-Entscheidungsfragen (D-01–D-16). **Fable-Entscheidungsrunde** darauf aufbauend: alle 16 Fragen entschieden, `docs/steering/design/TAILWIND-APP-BAUKASTEN_KONZEPT_V0-1.md` (von Albert freigegeben 2026-07-12) + Visual Board + App-Mockups (3 Archetypen) als neuer verbindlicher Design-Vertrag der gesamten App-Fabrik (25+ Apps). Kernentscheidungen: `fw-app` als Scope-Anker + Tailwind-Utilities (Hybrid), Section/Panel/Card-Taxonomie, Ein-Container-Vertrag am App↔Chart-Übergang, Chart-Chrome nach realer `FwRenderer`-Slot-Reihenfolge (Titel→BAN→Toolbar→Legende→Canvas), Donut-Ist-Bild (cutout 70 %, Center-Text), 4 Button-Varianten, `sm/md/lg` für generische UI (App-Mechanik-Sonderbreakpoints bleiben lokal), Tailwind-Defaults für Spacing/Radius + nur `shadow-soft`/`-hover` als CI-Zusatzstufen, JS-Klassen-Konstanten statt `@apply`, Literalregel für Build-Sicherheit (löst das im Befund identifizierte R-05/D-14-Risiko auf), `--fw-space-*` wird ersatzlos abgelöst (Umsetzung = `AP-tailwind-02`). **Parallel:** Design-Ablage konsolidiert — Jahrgang 2026-05 (`docs/design-system`, 35 Dateien, u. a. verbotene Farbnamen `-tint/-20/-80` mit abweichender Bedeutung ggü. `tokens.css`, Petrol-50-Namenskollision) nach `Archiv/design-system-2026-05/` verschoben (mit Schutz-README), `docs/design-system/` nur noch Statuskarte + Spec 05 (Icons). `NAVIGATION.md` (Design-Routing) und `BACKLOG.md` (DS-015 neu, `AP-tailwind-02` neu, DS-012/DS-014 nachgeführt) durch die Fable-Session direkt aktualisiert. Übergabe an Sonnet für `AP-tailwind-02` (Pilotmigration `prokrastinations-preis`) vorbereitet: `docs/steering/handovers/SONNET-PROMPT_AP-tailwind-02_pilotmigration_V1.md`. Einziger Vorfall: `.git`-Index-Korruption durch `git mv` aus der Cowork-Sandbox über das NAS-Laufwerk, am Host per `git read-tree HEAD` repariert, Working Tree unversehrt — Konsequenz: keine git-Schreibbefehle aus der Cowork-Sandbox auf das NAS-Repo. Kein Layer-1–5-Engine-Code, kein `app.js`/`app.css`/`screen.css`/`tokens.css`-Diff — reine Konzept-/Doku-/Ablage-Arbeit. Ergebnisprotokoll: `docs/steering/patches/AP-tailwind-01_befund-und-forschung_Ergebnis.md`. Kein Commit in dieser Kette — Commit-Message liegt vor, Freigabe liegt bei Albert. | 2026-07-12 | AP-tailwind-01 → Fable-Runde |
| AP-apptest-01 + AP-apptest-02 | App-Fabrik/Test | Testseiten-Handoff-Lücke geschlossen, unabhängig geprüft. Der reale App-Bau-Prozess (`tech-spec-app`, `04_CLAUDE_WORKFLOW_DRAFT.md`, `manual-test-plan`) verwies nirgends auf die fertige Testumgebung (`docs/testing/TEST_PAGE_STANDARD.md`, Template, Checker, 16 GRÜNE Testseiten); `manual-test-plan` referenzierte zusätzlich tote Pfade (`Theme/chart-tests/`, `Theme/data/`, seit `TESTENV-1eB` migriert). **AP-apptest-01 (GRÜN):** 4 Dateien chirurgisch ergänzt — `TEST_PAGE_STANDARD.md` §14 (`--write-index` nach grünem Check + Verbot der Handpflege von `tests/index.html`, Standard-Version 2→3), `tech-spec-app` (Handoff-Hinweis nach Abschnitt 16: Testfälle sind Grundlage für `app.test.html`, Bau folgt ausschließlich dem Standard, `tech-spec-app` baut selbst keine Testseite, `QA_TEST_CASES.md` nur bei realer Komplexität), `04_CLAUDE_WORKFLOW_DRAFT.md` (neuer Unterpunkt 5.1b in Phase 5: Template/Zielpfad/Checker/Launcher/Browserprüfung), `manual-test-plan` (tote Pfade auf reale — `Apps/{slug}/app.test.html`, `tests/engine/`, `tests/fixtures/engine/` — ersetzt). `app-spec-create` bewusst unverändert (Testfälle bereits über den bestehenden 18-Pflichtabschnitte-Check abgedeckt, Zusatzpunkt wäre Duplikat). **AP-apptest-02 (GRÜN, unabhängige Claims-vs-Files-QA, frischer Kontext):** alle vier Änderungen wörtlich gegen realen Diff und Strukturchecker bestätigt — GRÜN im ersten Anlauf, keine Reparatur nötig, Abschlussritual freigegeben. Kein Produktionscode, keine `Apps/**`-Datei, kein Checker-Code berührt. Sofort-erledigt-Pfad (kein BACKLOG-Eintrag, direkt archiviert). Ergebnisprotokolle: `docs/steering/patches/AP-apptest-01_testseiten-handoff-minimal_Ergebnis.md`, `..._02_unabhaengige-claims-vs-files-QA_Ergebnis.md`. Committed als `27deffa` („feat(AP-apptest-01+02): Testseiten-Handoff minimal verankert, unabhängig geprüft"). | 2026-07-11 | AP-apptest-01/02 |
| TESTENV-1 (inkl. TESTENV-1-FOLLOWUP-BTNFONT/-H3LEAK) | App-Fabrik/Test | **Initiative: Konsolidierte Testumgebung — vollständig abgeschlossen.** Phasen 1–4 (Inventar `TESTENV-1a-1aFRF`, Vertrag `1b-1bRF`/`docs/testing/TEST_PAGE_STANDARD.md`, Infrastruktur `1c/1cF`, Klassifikation `1d`) GRÜN. Phase 5 (Migration): App-Testseite (`1eA`, 32 Testfälle erhalten, Browser-Smoke von Albert bestanden) + 13+2 Engine-Zielseiten/76 Fixtures (`1eB`, GRÜN) migriert. Abschlussgate (`1f`): automatisches Gate GRÜN (16 Testseiten, 0 Strukturfehler), `.gitignore` bereinigt. Dabei erweitertes `tools/ci-token-check.js` (`fwCiAudit()`, primitives Ist/Soll-Set-Audit aus `tokens.css`, kein Hardcode) fand 3 CI-Bugs: `.fw-app__btn` Arial statt Source Sans Pro (`-BTNFONT`), `h3.fw-chart-title`-Leak aus bare `h3`-Legacy-Selektor (`-H3LEAK`), `.financial-chart-module`-`#ddd`-Rahmen (`-BORDER`, bewusst bis Tailwind zurückgestellt, bleibt eigene offene BACKLOG-Zeile). **`TESTENV-1g` (GRÜN, 2026-07-11) hat `-BTNFONT` und `-H3LEAK` behoben:** `app.css` `.fw-app__btn` um `font-family: inherit;` ergänzt (1 Zeile). H3-Leak betraf entgegen der ursprünglichen Vermutung nicht 1, sondern 4 identische Legacy-`<style>`-Blöcke (`tooltip.test.html`, `cadence-density.test.html`, `irregular-bar.test.html`, `irregular-line.test.html`) — zunächst an der im Auftrag gesetzten harten 3-Dateien-Grenze gestoppt und gemeldet, danach von Albert im selben AP auf 4 erweitert. Fix: bestehender Container `.fw-test-case` wiederverwendet, Kombinator auf Direkt-Kind verengt (`h3` → `.fw-test-case > h3`), trennt die gewollten Demo-Caption-Überschriften vom `.fw-test-expected h3` (2 Ebenen tiefer verschachtelt) und vom Engine-erzeugten `h3.fw-chart-title` (innerhalb `.kg-card`, kein Sektions-Direktkind) — keine neue Wrapperstruktur, kein `!important`. Zusätzlich `tests/shared/test-page.css` `.fw-test-expected h3` um `color: var(--color-text, #272727)` ergänzt (Redundanz-Absicherung). Beide Fixes automatisiert (`check-test-pages.py` GRÜN, Python-Regex-Nachweis auf Selektor-Statement-Ebene) und von Albert browserseitig verifiziert (Button-Font korrekt, orange H3 weg, `fwCiAudit()`-Stichprobe auf `minmax.test.html` bestätigt keinen Leak auf `h3.fw-chart-title`). Kein Layer-1–5-Engine-Code, kein Produktions-JS über die gesamte TESTENV-1-Kette geändert. `TESTENV-1-FOLLOWUP-BORDER` bleibt eigenständig offen (Tailwind-gekoppelt, unverändert). `TESTENV-1eB`/`1f`/CI-Audit-Tool bereits als Commit `ab95cfd` im Repo (git log verifiziert); `TESTENV-1g` uncommitted, Commit-Freigabe liegt bei Albert. Alle Protokolle unter `docs/steering/patches/TESTENV-1*_Ergebnis.md`. | 2026-07-11 | TESTENV-1a–1g |
| AP-prokrast-17-FOLLOWUP-FONT | Design | Font-Migration Pilot `prokrastinations-preis` erledigt: `--fw-font-base` (Fallback `sans-serif`, nirgendwo definiert) → Kontrakt-Token `--font-body`/`--font-display` (`tokens.css`) — umgesetzt durch AP-prokrast-17-FONT-CODE-A (Canvas) + AP-prokrast-17-FONT-CODE-B (HTML-UI, `app.css` Z.13-14 `--fw-font-base`→`--font-body`, App-Text/Headlines/A11y/Meldungen), Spec-Status durch AP-prokrast-17-FONT-SPEC-HEBUNG auf „implementiert (Mechanismus + Pilot)" gehoben. Albert-browserverifiziert (`app.test.html`, alle 5 HTML-UI-Flächen `document.fonts.check` ✅, `fwFontCheck()`-Tool-Aufwertung). **Abweichung von der ursprünglichen Kopplungsauflage:** Dieser BACKLOG-Eintrag verlangte ursprünglich „Kein reiner Rename — MUSS gemeinsam mit / vor der Rubikon-Nachmessung DS-FOLLOWUP-07 laufen (gekoppelt)". Real umgesetzt wurde die Migration **zuerst**, die Rubikon-Nachmessung **separat und offen belassen** — von Albert im AP-prokrast-17-FONT-CODE-B-Auftrag explizit als Nicht-Ziel benannt und autorisiert (Entkopplung). DS-FOLLOWUP-07 bleibt daher offen im BACKLOG, jetzt sachlich fällig statt nur vorsorglich. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17-FONT-CODE-A-CANVAS_Ergebnis.md`, `..._REVIEW_Ergebnis.md`, `..._CODE-B-HTMLUI_Ergebnis.md`, `..._SPEC-HEBUNG_Ergebnis.md`. | 2026-07-11 | AP-prokrast-17-FONT-SPEC-HEBUNG (Doku-Sync) |
| AP-prokrast-17-FONT-CODE-A (CANVAS-Write + unabhängiges REVIEW) | Engine/Design | Kein BACKLOG-Punkt, direkt archiviert (Pfad-A-Paar gemeinsam; das Write-AP-Ritual war im Vorfaden ausgefallen, hier mitgeschlossen). **CANVAS-Write (Pfad A, Canvas-Font-Migration):** Die Chart-Engine zieht die Schrift jetzt wie die Farben aus `tokens.css`. `FwTheme.init()` liest zusätzlich `--font-body`/`--font-display` (KDR 14 Punkt 5, Z.151-152), Constructor-Hardcode Z.57-60 bleibt Fallback. `FwLayoutRules.getResponsiveFont(ctx, family)` nimmt die Schrift als Parameter aus der injizierten init()'ten Theme (`resolvedFamily = family \|\| _theme.fonts.body`); Privatinstanz nur noch Fallback. Drei tote Font-Übergabewerte lebendig verdrahtet (Y-Achse `fontConfig.family`, Tooltip `styleConfig.title/bodyFont.family`, X-Achse Line), Bar-Y von nested `font:{family}` auf flache `family`-Form migriert. 6 Engine-Dateien: `FwTheme.js`, `FwLayoutRules.js`, `FwSmartXAxis.js`, `FwSmartYAxis.js`, `FwSmartTooltips.js`, `BarChartStrategy.js`. **Unabhängiges Claims-vs-Files-REVIEW (GRÜN, Opus, frische Instanz, nicht der Autor):** alle 6 Prüfaufträge gegen die realen uncommitteten Dateien bestätigt — `init()`-Font-Read real im getComputedStyle-Fluss; deterministisch 5 `getResponsiveFont`-Aufrufstellen, alle zwei-argumentig, 0 ohne `family`; `family` an jeder Stelle aus `t.fonts.body` (Line 419/422/388-389, Bar 239/272/280-281, Pie ohne getResponsiveFont-Pfad — A7 CenterText 314-315); Plumbing FwSmartScales→FwSmartXAxis/YAxis intakt; **Null-Delta** belegt (`tokens.css` `'Source Sans Pro'`/`'Archivo Black'` ≙ Constructor-Fallback, nur Quote-Stil → metrik-neutral); Scope exakt 6 Engine-Dateien (+ session-log Schritt 0); A6 (`FwChartTextPlugin.js:95` hart `sans-serif`) unberührt; keine Fonts im fwContext; keine Tabu-/Vault-/`FwDateUtils`-Berührung; Spec-Status (KDR 14 P5 / TECH-SPEC §5.4 / CI-POOL §9) durchgängig ehrlich „Code-Umsetzung offen", nicht vorzeitig gehoben. Keine Abweichung. Beobachtungen (kein Fix): Bar-A2 hartes `size:12` (vorbestehend, out of scope), `this.fonts.heading` noch ohne Canvas-Consumer (Parität für Write-AP B). Code + Review-Doku uncommitted (Commit-Freigabe + Spec-Status-Hebung = Alberts nächster gegatterter Schritt). Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17-FONT-CODE-A-CANVAS_Ergebnis.md`, `..._REVIEW_Ergebnis.md`. | 2026-07-10 | AP-prokrast-17-FONT-CODE-A-REVIEW |
| AP-prokrast-17-FONT-ANAMNESE + AP-prokrast-17-FONT-SPEC-PARITÄT | Design/Spec | Kein BACKLOG-Punkt, direkt archiviert. **FONT-ANAMNESE (GRÜN, reiner Lese-AP):** breite Font-Einbindungs-Inventur (26 Dateien, Python-deterministisch: `@font-face`, `font-family`, `--font-*`/`--fw-font*`-Token, Import-/Link-Ketten). Kernbefund: die reale Bruchstelle ist kein fehlender Bridge, sondern ein Namens-Mismatch — `app.css` referenziert `--fw-font-base`, das SSoT-Token heißt `--font-display`/`--font-body` (`tokens.css`); Ersteres ist repoweit nirgendwo definiert. Zusatzfund: die Chart-Engine hat eine zweite, nie CSS-gebrückte Font-Quelle (`FwTheme.js` `this.fonts` hardcoded, `init()` liest nachweislich nur Farben, nicht wie der Klassenkommentar suggeriert auch Fonts). `styles.css`/`stylesheet.css` beide tot bestätigt (nur DEPRECATED-Kommentar, keine reale Referenz mehr, `NAVIGATION.md`-Eintrag dazu veraltet). **FONT-SPEC-PARITÄT (GRÜN, Full-Gate, Diff vor Write gezeigt, Alberts OK abgewartet):** Fonts auf denselben KDR-14-Zielmechanismus wie Farben gehoben, mit durchgängig ehrlichem Status „Code-Umsetzung offen" — `ARCHITECTURE STRATEGY PAPER VX.md` (KDR 14 neuer Punkt 5 + Heading-Qualifier), `TECH-SPEC Theme-Integration Chart-Engine.md` (§5.4 umgeschrieben: Ist-Zustand lokal/DSGVO bereits real seit AP-16, veraltete Google-Fonts-CDN-Empfehlung entfernt, Ziel-Mechanismus mit Cross-Ref zu KDR 14 ergänzt), `CI-POOL-ROLLENKONTRAKT.md` (§9 neuer Font-Bridge-Zielmechanismus-Absatz nach Z.253, Z.251/253 wortgleich stehen gelassen, Stand-Header aktualisiert). Farb-Staleness TECH-SPEC Z.286 („CSS-Bridge geplant, noch nicht implementiert") bewusst nur geflaggt, nicht gefixt (Alberts Entscheidung, ggf. separater 1-Zeilen-Minifix). Body-QA aller 3 Dateien nach Write bestanden. Nachtrag: Verdacht „toter Querverweis" auf die Anamnese-Ergebnisdatei geprüft (Python) — Referenz war byte-identisch mit dem realen Dateinamen, Verdacht unbegründet, kein Fix nötig. Über beide APs geänderte Dateien: nur die 3 genannten Specs (kein Code-/CSS-Wert-Diff, keine Tabu-Zone). `AP-prokrast-17-FOLLOWUP-FONT` (BACKLOG) bleibt offen — diese Vorarbeit liefert nur Bruchstellen-Verifikation + Spec-Ziel, keine Code-Migration; Quelle-Spalte dort ergänzt. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17-FONT-ANAMNESE_font-einbindung-breite-inventur_Ergebnis.md`, `..._17-FONT-SPEC-PARITAET_Ergebnis.md` (inkl. Nachtrag). | 2026-07-10 | AP-prokrast-17-FONT-ANAMNESE/SPEC-PARITÄT |
| AP-prokrast-16-FOLLOWUP-B (aufgelöst durch AP-18/19) | Engine/Doku | Property-Zahl-Widerspruch Theme-Bridge geklärt: `FwTheme.init()` liest **19** `--color-*`-Properties; das `colors`-Objekt hat **20** Keys, der zwanzigste (`linesDark`) wird aus `textSec` abgeleitet, nicht aus CSS gelesen. Die 16c-QA maß richtig (19); der Kontrakt hatte Farb-Tokens gezählt und sie fälschlich „gelesene Properties" genannt. Unabhängig belegt in AP-prokrast-18 (§2), Kontrakt-Wortlaut korrigiert in AP-prokrast-19 (§9/P15, Z.253). Nebenbefund: Claim steht in §9, nicht §8 (Backlog-§-Referenz war falsch, mit dieser Archivierung erledigt). | 2026-07-10 | AP-prokrast-18/19 |
| AP-prokrast-17b/17c/18/19 | QA/Doku/Review | Kettenabschluss der QA-Doku- und Review-Nachlaufarbeiten nach AP-17. **AP-17b (GRÜN):** `QA_TEST_CASES.md` Gruppe N (TC-N01–N09) für die AP-17-Farbmigration ergänzt (Token-Auflösung, Petrol-Primary, Error-Null-Delta, CSS-Leak, Altlast-Check, S/M/L-Matrix, Font-Stabilität); drei TCs ehrlich als offen markiert statt fälschlich abgenommen. **AP-17c (GRÜN):** vorbestehender Doku-Bug behoben — Testgruppen-Übersicht fehlte Gruppe P (Pulse); Body/Tabelle jetzt 1:1 (A–M, P, N), Buchstabenlücke „O" als vorbestehend gemeldet. **AP-18 (GELB, Opus, unabhängiges Claims-vs-Files-Review AP-15a–17c):** alle 8 load-bearing Substanz-Behauptungen bestätigt (GRÜN) — Null-Delta 19/19, Composition-Root/Constructor-Injection, Tabu-Zonen unberührt; Property-Zahl-Rätsel 20 vs. 19 deterministisch aufgelöst; fünf Doku-Falschaussagen gefunden (`screen.css` fälschlich „Engine-intern" genannt, obwohl produktives Ghost-Stylesheet; TC-N05 strukturell nie auslösbar, kein `.hbs` im Repo; `NAVIGATION.md`-Hex-Regel zu absolut; Spec nennt `FwTheme`-Renderer statt `FwRenderer`; `screen.css`-Kopfkommentar veraltet). **AP-19 (GRÜN):** fünf AP-18-Folgeempfehlungen umgesetzt — Kontrakt-Property-Zahl korrigiert, TC-N05 neu formuliert (App-Card-Kaskade statt nie auslösbarer `.hbs`-Check) + Korrektur-Nachtrag im 17b-Protokoll (Original unverändert, Forensik-Prinzip), `NAVIGATION.md`-Hex-Regel entschärft (`#f9fafb`/`#f3f4f6` als dokumentierte Neutral-Dubletten bis T1), `screen.css`-Kopf synchronisiert, Spec-Wortlaut korrigiert. Alle drei normativen Dateien per spec-rewrite-guard-Diff vor Write gezeigt, Albert-OK abgewartet. Über die gesamte Vierer-Kette geänderte Dateien: `QA_TEST_CASES.md`, `CI-POOL-ROLLENKONTRAKT.md`, `NAVIGATION.md`, `screen.css` (nur Kopfkommentar), `ARCHITECTURE STRATEGY PAPER VX.md`. Kein Code-/`.js`-Diff, kein CSS-Wert-Diff, keine Tabu-Zone berührt. Vertagt: Hex-Tokenisierung → T1, Harness-Versionierung → TESTENV-1. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17b_qa-testcases-ci-farbtoken_Ergebnis.md`, `..._17c_qa-uebersicht-gruppen-sync_Ergebnis.md`, `..._18_claims-vs-files-review-gesamtkette_Ergebnis.md`, `..._19_abschluss-korrektur-doku-falschaussagen_Ergebnis.md`. | 2026-07-10 | AP-prokrast-17b/17c/18/19 |
| AP-prokrast-17 | App/CSS/Design | Pilot-Migration `prokrastinations-preis`: CI-Farbtokens `--fw-color-*` → Kontrakt-Rollen-/Farbtokens `--color-*` (`docs/steering/design/CI-POOL-ROLLENKONTRAKT.md` §1/§8; `tokens.css` = SSoT). APP-ARBEIT + Full-Gate, zweiphasig (Anamnese/Mapping-STOP → Write). `app.css`: 24 CI-Farb-Referenzen / 8 Tokens migriert (`text, bg, primary, muted→text-muted, surface, error-text/-border/-bg`), Fallbacks = Kontrakt-FINAL, deterministischer Python-Replace mit Count-Assertions + 1 Doku-Header. `app.test.html`: 1 `<link>` auf `tokens.css` VOR `app.css` (Fork 1 Variante A, nur Dev-Harness — Ghost/Prod bleibt T1). Anamnese fand **5 Wertdrifts statt der im Startprompt erwarteten 1**: `primary` Blau `#0071bf`→Petrol `#218380` (Kontrakt-§1.1-Anomalie-Korrektur, sichtbar, ΔE 52), `muted #555→#666` (P16), `text #1a1a1a→#272727`, `surface #f5f5f5→#fafafa` (klein/minimal), plus **Font-Konflikt**: `--fw-font-base`-Migration widerspräche Kontrakt §9 (Font-Ausklammerung + Rubikon-Metrik DS-FOLLOWUP-07) → §8-Stop, per Fork aufgelöst. 5 Forks von Albert entschieden (1=A · 2=vertagt · 3=ja · 4=ja · 5=Font nicht migrieren). Bewusst NICHT geändert: `--fw-font-base`, `--fw-space-*` (T1), Mechanik-`--fw-*` (Tabu §4), `app.js`, Engine/Theme/Specs. Vier-QA + Albert-Live-Farbabnahme + Konsolen-ΔE-Gegenprobe (alle 5 Tokens Match ✅, `tokens.css` greift live). Dauerhaftes generisches QA-Tool `tools/ci-token-check.js` angelegt. Offen weitergegeben: Screenreader-Volltest + S/M/L-Viewport-Matrix; Nicht-Ziele (Font-Migration DS-FOLLOWUP-07, `--fw-space-*`/Ghost-Anbindung T1). Kein Commit (HEAD `1b5c283`; nur AP-17 uncommitted). Ergebnisprotokoll: `docs/steering/patches/AP-prokrast-17_pilot-migration-prokrastinations-preis_Ergebnis.md`. | 2026-07-09 | AP-prokrast-17 |
| AP-prokrast-kdr14 (BACKLOG AP-prokrast-16-FOLLOWUP erledigt) | Design/Spec | KDR-14-Wortlaut-Nachführung in `docs/spec/ARCHITECTURE STRATEGY PAPER VX.md` — chirurgischer Sync von Wortlaut auf den in AP-16/16c real umgesetzten Stand, kein Code berührt, kein Prinzipienverlust (Init-Reihenfolge Z.150 und Begründung Z.152 unverändert gültig). 4 Textstellen geändert: Z.55 (Datei-Matrix, Theme-Zelle: „aktuell hardcoded; geplant" → „aus `tokens.css`, implementiert und durchgeleitet"), Z.148 (Architektur: Quelle `screen.css`→`tokens.css`, + Composition Root `ChartEngine`, Constructor Injection, Graceful Default), Z.149 (Status: „Implementiert" → „Implementiert und durchgeleitet (AP-16/16c)"), Z.151 (Konsequenz: „Änderung in `screen.css :root`" → „Änderung in `tokens.css`, SSoT seit AP-16; `screen.css` bindet per `@import` ein"). Nachtrag (Ein-Zeilen-Korrektur, Z.149): konkrete Zahl „19/19" entfernt und durch qualitativen Beleg „Null-Delta-QA, AP-16c" ersetzt — Grund: Widerspruch zwischen Spec und Kontrakt (`CI-POOL-ROLLENKONTRAKT.md` §8/P15 nennt 20 gelesene `--color-*`-Properties, AP-16c maß 19); zugrundeliegende Property-Zahl-Diskrepanz bewusst nicht aufgelöst, sondern als eigener BACKLOG-Punkt AP-prokrast-16-FOLLOWUP-B registriert. Version-/Datum-Header der Spec (`12.0.0`, `17.02.2026`) bewusst nicht gebumpt (GELB-Notiz, separate Entscheidung Alberts). Ergebnisprotokoll: `docs/steering/patches/AP-prokrast-kdr14_spec-wortlaut-nachfuehrung_Ergebnis.md`. | 2026-07-09 | AP-prokrast-kdr14 |
| AP-prokrast-15a–16c (+ DS-015 erledigt) | Engine/Design/CSS | CI-/Theme-Bridge Umsetzung — von Inventar bis Theme-Durchleitung an die Chart-Engine, Null-Delta belegt und am Live-Server-Harness von Albert verifiziert. Löst DS-015 (Farbrollen-Entscheidung, war Blocker für AP-15). **AP-15a (Tailwind-CI-Pool-Inventar, GRÜN):** reale Farben/Fonts/Elemente/Renderpfade read-only kartiert; geschlossenes CI-Fundament (Petrol/Blau/Gelb/Purpur + Neutral/Error + Archivo Black/Source Sans Pro) über `screen.css`/`FwTheme.js`-Fallbacks/Design-System-Templates identisch; Farbfächer doppelt (statische CSS-Ramp + dynamisch `FwTheme.getGhostColor()`/`getColor()`); zwei neue Funde (fertiges Tailwind-App-Template `docs/App-Fabrik/_input/perplexity/fw-app-template.html`; var()-vs-Hex-Duplikation in Design-System-Templates); 11 Entscheidungsfragen an Master. **AP-15b (Rollen-/Benennungskontrakt, GELB):** `docs/steering/design/CI-POOL-ROLLENKONTRAKT.md` NEU übersetzt die 11 Masterentscheidungen (Rücklaufkapsel `AP-prokrast-15a_ruecklauf_masterentscheidungen_F1-F11.md`) in verbindliche Regeln (Rollenmodell primary=Petrol/link=Blau/error-Set zentralisiert, erlaubte/verbotene Namensräume, Tailwind-Tokenstruktur mit gepinnten Seeds, Konsumenten-Regel, Nutzungsregeln je Familie, Migrationsreihenfolge); BACKLOG T1 ergänzt; GELB nur wegen Namensabweichung der Gate-Datei (`ruecklauf` statt `ruecklaufkapsel`, inhaltlich eindeutig identisch) — transparent gemeldet. **AP-15c (Farbleiter-Generierung + Abnahme-Board, GELB):** deterministischer Python-Generator (`docs/steering/patches/AP-prokrast-15c_farbleiter_generator.py`, OKLCH/Strategie B, WCAG-Kontrast, Gamut-Sicherung) erzeugte volle Leitern 50–900 Petrol/Gelb/Purpur (Seeds byte-identisch gepinnt) + autarkes Abnahme-Board; Ist-Wert-Verifikation korrigierte zwei fehlerhafte Rücklaufkapsel-Ableitungen (`gelb-80 #F9EF9E` statt `#E5D337`, `purpur-80 #C57EB2` statt `#A43585` — nur Petrol folgt dem Weißmisch-Gesetz); GELB nur wegen Petrol-Pin-Quasi-Gleichstand Teal-600/700 (Δ 0,0001 L, beide Optionen dokumentiert, nicht selbst umentschieden); Albert nahm am Board ab: Pin 600 bestätigt, Werte festgeschrieben. **AP-16 (Theme-Migration Stufe 1, GRÜN):** `Theme/assets/css/tokens.css` NEU als Single Source of Truth (15c-FINAL byte-identisch); `screen.css` per `@import` angebunden, `:root`-Farbblock vollständig entfernt, 4 Konsumenten umgestellt (2× `color-mix`, petrol-20→200, Caption `#666`→`--color-text-muted`); `FwTheme`-Bridge + Fallback-Konstanten auf neue Stufen nachgezogen (Palette-Slots unverändert; Mapping petrol-80→500/-50→400/-20→200, gelb-80→200, purpur-80→400 per OKLCH-nächste unter Nutzungsregel-Vorbehalt); zwei Plugin-Hartcodierungen aufgelöst (`FwVerticalLinePlugin` blau, `FwAnnotationPulsePlugin` `#006273`→Petrol); Kontrakt §8 FINAL/§4.3 Pin 600, Erratum in Rücklaufkapsel. **AP-16b (Bridge-Konsolidierung + Kaskaden-Harness, GRÜN, spec-getriggerte Kurskorrektur):** erster Injektionsweg über `ChartEngine.js` (Layer 2, `this.renderer.theme`) verstieß gegen KDR 14.2/Layer-Disziplin (`docs/spec/ARCHITECTURE STRATEGY PAPER VX.md`, `Der Rucksack`) — nach Alberts Spec-Verweis vollständig revertiert und spec-konform in `LineChartStrategy` (Layer 3, „Strategy übergibt Tokens") verlagert; Plugins lesen nur noch injizierte Options mit defensivem Fallback, kein `getComputedStyle` mehr außerhalb `FwTheme.init()`; neuer Kaskaden-Harness `Theme/chart-tests/AP-16-abnahme.html` (lädt screen.css/tokens.css + lokale `vendor/chart.umd.min.js`, kein CDN, Kaskade-Indikator); Testdaten-Refresh 6 alte chart-tests (19 Hex-Ersetzungen); dabei vorbestehender Architektur-Befund entdeckt (Strategien nutzten eigene, nie init()'te FwTheme-Instanzen → Chart-Farben aus JS-Fallbacks). **AP-16c (Theme-Durchleitung, GRÜN, Albert-verifiziert):** Composition Root + Constructor Injection mit Graceful Default; Archäologie (Chesterton's Fence) fand keinen dokumentierten Gegen-Grund, Wurzel = unvollständige CSS-3-Migration; `ChartEngine`-Konstruktor (protected, begründet) erzeugt Renderer vor Strategien und reicht dessen init()'te `FwTheme`-Instanz an alle drei Strategien durch, `constructor(theme = new FwTheme())` erhält Standalone-Testbarkeit; **Null-Delta deterministisch belegt (19/19 --color-*-Werte byte-identisch tokens.css ↔ Fallback)**; Harness um Chart-Bridge-Indikator (Read-only-Identitäts-Check) erweitert; FwLayoutRules bewertet, nicht geändert (nutzt nur `fonts.body`, nicht CSS-gebrückt); KDR 14.3 unverändert, `FwTheme.js`/`FwRenderer.js`/`fwContext`/Specs nicht berührt; Albert am Live-Server abgenommen (beide Indikatoren LIVE, Charts unverändert, Fallback-Gegenprobe erfolgreich). Über die Kette geänderte produktive Dateien: `tokens.css` (neu), `screen.css`, `FwTheme.js`, `FwVerticalLinePlugin.js`, `FwAnnotationPulsePlugin.js`, `LineChartStrategy.js`, `BarChartStrategy.js`, `PieChartStrategy.js`, `ChartEngine.js`; `app.css`/`app.js` und `FwChartTextPlugin.js` NICHT berührt; keine `docs/spec/`-Änderung; Tabu-Zonen unberührt. Offen weitergegeben: KDR-14-Spec-Wortlaut-Nachführung (BACKLOG AP-prokrast-16-FOLLOWUP), CI-Font-Anbindung (DS-012/013/FOLLOWUP-07/08 — nur Farben gebrückt), AP-17 Pilot-Migration. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-15a_...`, `..._15b_...`, `..._15c_...`, `..._16_theme-migration_Ergebnis.md`, `..._16b_bridge-konsolidierung-kaskaden-harness_Ergebnis.md`, `..._16c_theme-durchleitung_Ergebnis.md`. | 2026-07-09 | AP-prokrast-15a…16c |
| RITUAL-OPT-1 | Steering/Infrastruktur | Abschluss-Ritual + PROJECT-STATUS tokensparend umgebaut. HOOK-META-Phantomfeld 'Nebenabschluss' (14k Zeichen, seit 2026-07-01 unbemerkt) entfernt + enforcing-Validator 'tools/check-project-status-hook-meta.py' (Feld-Whitelist/Längen/Blockgröße/Datei-Größen-Riegel) gebaut und unbedingt ins Ritual §3.6 + Writer verdrahtet. PROJECT-STATUS §1/§3/§8 getrimmt 108→19,4 KB (−82%). Read-freies Append-Tool 'tools/append-log-line.py' (Dublettensperre) + BACKLOG-ARCHIV-Invariante 'neueste unten' (Archiv-Read/Abschluss ~36k→0 Tokens, in §3.7 + Writer verdrahtet). Log-Rotation 'tools/rotate-log.py' (Jahres-Segmente) + distill Schritt7 löschen→archivieren (Rohlog erhalten). Sofort-erledigt, kein BACKLOG-AP. Punkte 6-9 offen als RITUAL-OPT-2. Uncommitted. | 2026-07-12 | RITUAL-OPT (Fable-Analyse→Umbau) |
| RITUAL-OPT-2 | Steering/Infra | Abschluss-Ritual-Optimierung Teil 2, alle 4 Punkte abgeschlossen. **Punkt 6 (NAVIGATION.md):** Writer nutzt jetzt `append-log-line.py` (Normalfall Append) bzw. neues `replace-matched-line.py` (Sonderfall 🟡→✅-Flip) statt Read+Edit — kein Voll-Read von NAVIGATION.md (18,5 KB) mehr im Ritual; `BACKLOG.md`-Zeilenentfernung ebenfalls auf `replace-matched-line.py` umgestellt. **Punkt 7 (session-start.ps1/start.md):** Hook extrahiert `Aktive-APs` (BACKLOG.md 🟡-Aktiv-Tabelle) und `Archiv-seit-Log` (BACKLOG-ARCHIV.md Datumsfilter) jetzt per PowerShell-Regex direkt — Haiku-Dispatch dafür entfällt im Kaltstart-Vollmodus; NAVIGATION.md bleibt bewusst beim Haiku-Dispatch (Fließtext, Blind-Regex-Risiko). PS-5.1-Fallstrick gefunden+behoben: Mehrbyte-Emoji-Literal im .ps1-Quelltext wird ohne BOM falsch geparst, Fix via `[char]::ConvertFromUtf32()`. **Punkt 8 (REGRESSION-MATRIX.md):** Automatik-Gate-Spalte ergänzt — Befund: kein einziger der 20 REG-Einträge hat ein automatisches Gate (Tool-Inventar geprüft: check-test-pages.py nur Struktur, ci-token-check.js manuelles DevTools-Snippet), Matrix ist ehrlich vollständig manuell, Versicherung bewusst behalten. **Punkt 9 (DECISION-LOG.md + BACKLOG-ARCHIV.md):** neues Tool `append-block.py` für mehrzeilige Decision-Blöcke (append-log-line.py passt nicht, lehnt Zeilenumbrüche ab); neues Tool `rotate-table-log.py` rotiert Markdown-Tabellen nach Datum statt Position (Reverse-Order-Altbestand-Risiko in BACKLOG-ARCHIV.md erkannt und im Tool-Design vermieden) — Erstrotation 219→12 aktive Zeilen, 207 nach `BACKLOG-ARCHIV-2026.md`. Regression gefunden+behoben: `kassensturz-archiv-query.py` las nur die aktive Datei, jetzt segment-aware (scannt Jahres-Segmente ≥ --since-Jahr); zusätzlich fehlendes UTF-8-Stdout-Encoding in demselben Skript behoben (Absturz bei Sonderzeichen in AP-IDs). Verifiziert: `--since 2025-01-01` liefert exakt 219 Treffer (volle Datenerhaltung). Ergebnisprotokolle: `docs/steering/patches/PATCH-RITUAL-OPT-2-Punkt6-2026-07-12.md` bis `..._Punkt9-2026-07-12.md`. Uncommitted, Commit-Freigabe liegt bei Albert. | 2026-07-12 | RITUAL-OPT-2 |

Stand: 2026-07-23 | Session: AF-GM-02c | Geändert von: Claude

# Patch-Quittung | AF-GM-02c | 2026-07-23

**Beauftragt:** `tools/golden-master/verify.mjs` um einen optionalen `--target-url`-Modus erweitern, der die gespeicherte Trace-Spur gegen eine explizit lokale HTTP-Loopback-URL statt gegen die referenzierte Fixture-Datei abspielt. Referenz-Hash bleibt immer zuerst und unverändert Vorrang; Ziel-URL ist keine neue Trace-Eigenschaft (Schema unverändert). Auftragsquelle: `Archiv/local/muss noch eingeordnet werden/PROMPT_CLAUDE_AF-GM-02C_TARGET_REPLAY_V1.md`.

**Geändert:** 6 Datei(en), 11 Stelle(n)

**Dateien:**
- `tools/golden-master/verify.mjs` — geändert, 5 Stellen (Fehler-ID `GM-ERR-TARGET-URL-NOT-LOCAL`, Locality-Whitelist + `parseLocalTargetUrl()`, CLI-Argument-Parsing `--target-url`, Locality-Check nach Hash-Check vor Browser-Start, Navigationsziel + PASS-Output um `targetUrl` ergänzt)
- `tools/golden-master/README.md` — geändert, 2 Stellen (neuer Abschnitt „Target-Replay gegen lokale URL", Stand-Zeile)
- `tests/golden-master/local-target-server.mjs` — neu (Loopback-Testhilfe, `node:http`, kein neues Paket)
- `tests/golden-master/target-replay-check.mjs` — neu (deterministischer Treiber: Server starten, `verify.mjs` als Kindprozess ausführen, Server garantiert schließen)
- `tests/fixtures/golden-master-target-replay/target-state-mismatch.html` — neu (synthetisches Ziel-Fixture für Negativnachweis Zielzustandsabweichung)
- `tests/fixtures/golden-master-target-replay/target-missing-selector.html` — neu (synthetisches Ziel-Fixture für Negativnachweis fehlender Zielselector)

**CHANGED/NEW-Markierungen:** ✓ in `verify.mjs` vollständig mit `// NEW`/`// CHANGED` markiert. Neue eigenständige Dateien (`local-target-server.mjs`, `target-replay-check.mjs`) ohne Zeilen-Marker (komplette Neuanlage, kein Marker-Zwang). `README.md` reine Doku (N/A).

**Tabu-Check:** keine ✓ — kein `forbidden`/`protected`-Pfad aus `.claude/PROTECTED_PATHS.json` berührt; `package.json`, `package-lock.json`, `behavior-trace.schema.json`, Theme-/Engine-Pfade unverändert.

**Gate-Typ:** Full

**Tatsächliche Tests (reale Kommandos, reale Exit-Codes):**

| # | Szenario | Kommando | Exit | Fehler-ID |
|---|---|---|---|---|
| 1 | Positivlauf, `minimal-normal` gegen lokales HTTP-Ziel | `node tests/golden-master/target-replay-check.mjs tests/fixtures/golden-master-trace fixture.html tests/golden-master/traces/minimal-normal.behavior-trace.json` | 0 | — `PASS`, `targetUrl` gesetzt |
| 2 | Zielzustandsabweichung | `node tests/golden-master/target-replay-check.mjs tests/fixtures/golden-master-target-replay target-state-mismatch.html tests/golden-master/traces/minimal-normal.behavior-trace.json` | 1 | `GM-ERR-STATE-MISMATCH` |
| 3 | Fehlender Zielselector | `node tests/golden-master/target-replay-check.mjs tests/fixtures/golden-master-target-replay target-missing-selector.html tests/golden-master/traces/minimal-normal.behavior-trace.json` | 1 | `GM-ERR-SELECTOR-NOT-FOUND` |
| 4 | Nichtlokale Ziel-URL | `node tools/golden-master/verify.mjs tests/golden-master/traces/minimal-normal.behavior-trace.json --target-url http://example.com/` | 1 | `GM-ERR-TARGET-URL-NOT-LOCAL`, kein Browser gestartet |
| 5a | Regression `minimal-normal` ohne `--target-url` | `node tools/golden-master/verify.mjs tests/golden-master/traces/minimal-normal.behavior-trace.json` | 0 | `PASS`, `targetUrl: null` |
| 5b | Regression `minimal-reduced` | `node tools/golden-master/verify.mjs tests/golden-master/traces/minimal-reduced.behavior-trace.json` | 0 | `PASS`, `targetUrl: null` |
| 5c | Regression `input-controls-normal` | `node tools/golden-master/verify.mjs tests/golden-master/traces/input-controls-normal.behavior-trace.json` | 0 | `PASS`, `targetUrl: null` |
| 6a | Regression Schema (4 Spuren inkl. neu) | `python tools/golden-master/validate_schema.py tests/golden-master/traces/minimal-normal.behavior-trace.json tests/golden-master/traces/minimal-reduced.behavior-trace.json tests/golden-master/traces/af-gm-03-synthetic.behavior-trace.json tests/golden-master/traces/input-controls-normal.behavior-trace.json` | 0 | `SCHEMA-VALIDIERUNG: GRUEN (4 Datei(en))` |
| 6b | Regression Paket `af-gm-03-positive` | `python tools/golden-master/validate_package.py tests/golden-master/packages/af-gm-03-positive` | 0 | `PASS` |

**Scope-QA (Nachweis 7):** `git status --short` nach dem Patch enthielt ausschließlich die sechs oben gelisteten Dateien im erlaubten Scope plus zwei fremde, vorbestehende Einträge außerhalb dieses APs (`.claude/learning/session-log.md` aus dem `/start`-Session-Start, eine unabhängig entstandene Chronik-Datei) — beide nicht Teil dieses Patches.

**Restabweichung / Lernpunkt:** Erster Implementierungsversuch von `target-replay-check.mjs` nutzte `spawnSync`. Das blockiert die Event-Loop des Elternprozesses vollständig, wodurch der dort laufende HTTP-Server während der Kindprozess-Laufzeit keine Anfragen mehr bedienen konnte — Chromium-Navigation lief real in einen 30-Sekunden-Timeout (`GM-ERR-UNEXPECTED`), kein Sandbox- oder Netzwerkproblem. Durch gezielte Prozess-Isolation (Server+Browser im selben Prozess lief; Server+Chromium über `spawnSync`-Kind lief nicht) lokalisiert und auf asynchrones `spawn` mit garantiertem `finally`-Server-Close umgestellt. Danach alle Nachweise grün.

**Offene Fragen:** keine. Grenze ausdrücklich benannt: dieser AP beweist keinen lokalen Ghost-Page-Replay und keine Produkt- oder Launch-Reife (bleibt AF-GM-04).

---

Zählprüfung: Ich habe 11 Stellen geändert. Aufzählen? Nein — vollständig oben dokumentiert.

→ Bitte teste mit den sieben Kommandos oben (insbesondere dem Loopback-Ansatz über `target-replay-check.mjs`). Ich warte vor dem nächsten Patch (Evidenzdatei `tests/golden-master/evidence/AF-GM-02C-NACHWEISE.md`) auf deine Bestätigung.

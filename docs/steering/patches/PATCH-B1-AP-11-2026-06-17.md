Stand: 2026-06-17 | AP: B1-AP-11 | Gate: Full

# Patch-Quittung — B1-AP-11

```
PATCH-QUITTUNG | AP B1-AP-11 | 2026-06-17
Beauftragt:    CSV + Stations-JSON parallel laden: loadStations() hinzufügen,
               Promise.all-Orchestrierung in initApp, stationsConfig an renderContent übergeben
Geändert:      1 Datei, 3 Stellen
Dateien:       Apps/prokrastinations-preis/app.js
CHANGED/NEW:   ✓ markiert (// CHANGED — AP-11 / // NEW — AP-11)
Tabu-Check:    keine ✓ — CSVParser.js, FinanzwesirData.js, ChartEngine-Dateien unberührt
Gate-Typ:      Full
Testfall:      app.test.html im Live Server öffnen
               - Normalfall: Screen 1 erscheint, kein [fw-app]-Error in Console
               - Fehlerfall JSON: DevTools → Network → Request blockieren für stations.de.json
                 → App zeigt Error-State (data-fw-state="error")
               - CSV weiterhin intakt: Slider + Weiter → Screen 2 mit Chart wie vorher
Offene Fragen: Relativer Pfad config/stations.de.json auf Ghost.io noch ungetestet
               (bekanntes AP-11-Risiko, kein Blocker für Lokaltest)
```

## Geänderte Stellen

| Stelle | Art | Beschreibung |
|--------|-----|--------------|
| `renderContent` Signatur (Zeile ~141) | CHANGED | 4. Parameter `stationsConfig` hinzugefügt (AP-12 verfügbar) |
| Nach `_loadDataImpl` (Zeile ~454) | NEW | `loadStations()` Funktion — fetch, HTTP-Check, JSON.parse, Error(d) |
| `initApp` Ladeblock (Zeile ~480) | CHANGED | `Promise.all([loadData(), loadStations()])`, `result` → `csvResult`, Stations-Error-Zweig, `stationsConfig` an `renderContent` |

Zählprüfung: Ich habe 3 Stellen geändert. Aufzählen: s.o.
→ Bitte teste mit app.test.html (Normalfall + JSON-Fehlerfall). Ich warte vor dem nächsten Patch.

# tests/scratch/

Lokale Wegwerf-Experimente. Nicht Teil des dauerhaften Testbestands.

- Wird vom Strukturchecker (`tools/check-test-pages.py`) nicht validiert.
- Erscheint nicht im Launcher (`tests/index.html`).
- Wichtige Erkenntnisse oder dauerhafte Tests müssen vor Abschluss an einen
  Standardort überführt werden (`Apps/{slug}/app.test.html`, `tests/engine/`,
  `tests/tooling/`, `tests/ghost/`).
- Keine produktiven Daten oder geheimen Informationen ablegen.

Siehe `docs/testing/TEST_PAGE_STANDARD.md` §1.4.

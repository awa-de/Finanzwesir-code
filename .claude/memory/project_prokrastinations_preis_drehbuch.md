---
name: project-prokrastinations-preis-drehbuch
description: "prokrastinations-preis: Drehbuch war Soll-Quelle für Screen 4; seit AP-prokrast-04a (2026-07-03) ist APP_SPEC.md §16.1a die synchronisierte, operative Spec für den gebauten Rubikon-Endstand; QA_TEST_CASES.md seit AP-prokrast-05a-05e (2026-07-03) ebenfalls synchron inkl. CTA-Fokus-Testabdeckung; Beat-2-✅/❓-Symbolik seit AP-prokrast-06a-06d (2026-07-03/04) entschieden und seit AP-prokrast-07a-07d (2026-07-06) fertig gebaut, feinjustiert und GRÜN abgenommen (FwChartTextPlugin.js anchor:'lastPoint', BACKLOG AP-26 archiviert); Screen 2 Card-to-Point-Motion seit AP-prokrast-08a-08c (2026-07-06) gebaut; Screen 3 (Kontinuitäts-Reveal statt wörtlichem Drehbuch-Timing-Beat) seit AP-prokrast-10a-10d (2026-07-07) gebaut, unabhängig geprüft, Rücklauf freigegeben; APP_SPEC.md/Drehbuch/QA_TEST_CASES.md seit AP-prokrast-11a-11d (2026-07-07) auf den AP-08/AP-10-Endstand synchronisiert — Drehbuch-Screen-3-Zitat trägt jetzt (analog Screen 4) einen Amtlicher-Hinweis-Block + HISTORISCHER_STAND/AKTUELLER_SOLLSTAND-Rahmung, ist also NICHT mehr unmarkiert veraltet; neues LLM-lesbares Forensik-Rahmungsmuster etabliert; zwei offene Folgepunkte DS-FOLLOWUP-07/08 (Font-Neumessung) + AP-prokrast-08-FOLLOWUP-A (Engine-Technical-Debt) unverändert offen; zusätzlicher Fund aus AP-prokrast-11c: Implementierungs-Notiz zu RubikonSymbolMarkers (Drehbuch Z.240) behauptet weiterhin fälschlich 'Bau noch offen' trotz AP-07-Fertigstellung, als BACKLOG AP-prokrast-11-FOLLOWUP registriert, bewusst nicht in AP-11 repariert"
metadata: 
  node_type: memory
  type: project
  originSessionId: a35e85df-d14e-477e-b4b3-0052c090219a
---

`Apps/prokrastinations-preis/drehbuch_prokrastinationspreis_app.md` (Stand 2026-06-25) ist das amtliche, freigegebene fachliche Zielbild für die App `prokrastinations-preis`. Für Screen 4 ist seit AP-prokrast-04a (2026-07-03) `APP_SPEC.md` §16.1a die synchronisierte, operative Spec-Quelle — sie beschreibt den tatsächlich gebauten, von Albert bestätigten stehenden Rubikon-Screen (kein Morph, kein C2, DOM-Overlay-Text), nicht mehr die ursprüngliche Drehbuch-Choreografie mit Achsenanimation. Seit AP-prokrast-11a–11d (2026-07-07) gilt dasselbe Muster für Screen 3: `APP_SPEC.md` §16.1b ist die operative Spec-Quelle für den gebauten Kontinuitäts-Reveal, das Drehbuch trägt dafür jetzt einen eigenen Amtlicher-Hinweis-Block. Card-to-Point (Screen 2, seit AP-prokrast-08a-08c gebaut) ist in `APP_SPEC.md` §16.1a als abgenommener Endstand dokumentiert, ohne eigenen Drehbuch-Ablöse-Block (Drehbuch beschrieb Card-to-Point nie als eigene Beat-Choreografie).

**Warum wichtig:** Die Drehbuch-Datei ist im Git-Status untracked. Ein Claude, der nur `git log`/`git status` prüft, würde sie leicht übersehen oder ihre Autorität anzweifeln. Der untracked-Status ist ausdrücklich nur ein Commit-Hinweis, keine fachliche Unsicherheit. Für Screen 4 gilt jetzt zusätzlich: Drehbuch-Beat 1 (Achsenanimation) und Beat 3 (Text unter dem Chart) sind im Drehbuch selbst als „abgelöst" markiert, Beat 2 (✅/❓-Symbolik) explizit als „offen, nie entschieden" — nicht als abgelöst verwechseln.

**Kernbefund aus AP-prokrast-02c (Architektur-Kontrakt):** Der bestehende `xDisplayRange`/`displayRange`-Mechanismus (Chart-Engine, AP-14b) trägt die Rubikon-Zukunftsraum-Mechanik (Screen 4) bereits strukturell — keine Obergrenzenprüfung in `LineChartStrategy.transform()`, `FwVerticalLinePlugin` positioniert sich bereits am letzten echten Datenpunkt. Card-to-Point (Screen 2, Kartenschrumpf-Animation) hat dagegen keine bestehende Koordinaten-Schnittstelle — höchstes Architekturrisiko im gesamten Drehbuch, weiterhin ungebaut.

**`QA_TEST_CASES.md`-Sync abgeschlossen (AP-prokrast-05a–05e, 2026-07-03, BACKLOG AP-27 erledigt):** `Apps/prokrastinations-preis/QA_TEST_CASES.md` Gruppe F ist jetzt vollständig mit `APP_SPEC.md` §16.1a synchron. TC-F01 behandelt den Rubikon-Chart nicht mehr als Fehler (alte Formulierung „Kein Zukunftschart." entfernt). TC-F03/TC-F04 decken DOM-Haupttext, 800ms-Timing und — nach einem in AP-05b gefundenen und in AP-05c geschlossenen Nachputz — die CTA-Nicht-Erreichbarkeit während beider 800ms-Pausen (inkl. Reduced-Motion-Invarianz) testbar ab. Ausschließlich `QA_TEST_CASES.md` geändert, kein Code/Spec/Drehbuch.

**RubikonSymbolMarkers gebaut (AP-prokrast-07a, 2026-07-06):** `FwChartTextPlugin.js` additiv um `anchor:'lastPoint'` erweitert — bindet ✅/❓ an denselben Datenpunkt wie `FwVerticalLinePlugin` (`chart.getDatasetMeta(0)`, letzter Punkt), keine eigene/divergierende Zeitberechnung, kein DOM, keine A11y-Semantik. `app.js` `renderScreen4Chart()` ergänzt.

**CSS-Feinjustierung + Abschluss (AP-prokrast-07b–07d, 2026-07-06):** Albert fand beim manuellen Live-Server-Test zwei Layout-Defekte am DOM-Textblock `rubikonText` — Kollision mit „?" auf Screen S, fehlende gemeinsame Fluchtlinie zu „?" auf allen Breiten. Behoben über iterative DevTools-Messung ([[feedback_css_custom_properties_tuning]], Skript `tools/rubikon-symbol-markers-diagnose.js`) und eine neue M-Breakpoint-Stufe in `app.css` (481–1024px, zusätzlich zu den bestehenden S/L-Regeln, da eine gemeinsame Basisregel M und L nicht gleichzeitig treffen konnte). Unabhängige Abschluss-QA (AP-07b, GELB) fand M zunächst offen und den formalen DOM-/Accessibility-Tree-Check noch nicht dokumentiert — sowie den Gegenbefund, dass `CSS-KONVENTIONEN.md` nur für `screen.css` gilt, nicht für App-eigene Stylesheets. QA-Nachtrag (AP-07d, GRÜN): Albert nahm M unter dem aktuellen Fallback-Font-Stand bewusst ab (Feinjustierung erst nach CI-Font-Anbindung sinnvoll) und führte den DOM-/Accessibility-Tree-Check durch (kein DOM-Treffer, kein Accessibility-Tree-Eintrag, keine Live-Region-Aktualisierung) — TC-F05 damit für den aktuellen Font-Stand bestanden, Commit aus AP-07-Sicht freigegeben. **Wichtiger Fund (bleibt gültig):** DOM-Text und Canvas-„?" nutzen aktuell beide Browser-Default-`sans-serif`, nicht die offiziellen CI-Fonts ('Source Sans Pro'/'Archivo Black' sind im Theme längst per `@font-face` geladen, aber `--fw-font-base` ist nirgendwo definiert — Theme-Bridge-Lücke, BACKLOG DS-012/DS-013). Sobald Fonts angebunden werden, ändern sich Zeichenbreiten — komplette Neumessung nötig, als BACKLOG DS-FOLLOWUP-07 registriert. `app.js` darf laut `APP-INTERFACE.md` §4 keine Chart.js-Pixelinternals lesen, deshalb bleibt die Positionierung statisch in CSS statt live berechnet.

**Offene Punkte nach AP-prokrast-07 (BACKLOG AP-26 archiviert, Stand 2026-07-06):** Kein AP-07-Blocker mehr offen. Nachfolgepunkt DS-FOLLOWUP-07 (Rubikon-Neumessung nach CI-Font-Anbindung, hängt an DS-012/DS-013).

**Card-to-Point Screen 2 gebaut (AP-prokrast-08a–08c, 2026-07-06):** Damit ist auch der zweite der beiden im Drehbuch als architektonisch riskant markierten Bausteine gebaut. Analyse (AP-08a, GRÜN) bestätigte: kein Chart.js-Internals-Zugriff aus `app.js` nötig, neues eigenständiges `FwAnchorMeasurementPlugin.js` als Lösung. Implementierung (AP-08b, GELB) + Sequenzkorrektur (AP-08b2) + Chart-Settled-Gate (AP-08b3) + Architektur-Gate (AP-08b4a) + RenderMotion (AP-08b5) + harte Abschluss-QA (AP-08c, GELB) — volle Herleitung inkl. der neuen Engine-Contracts (`anchorMeasurement`, `chartSettled`, `renderMotion`) und der Chart.js-Plugin-Registrierungs-Plattformgrenze in [[project_chartengine_anchormeasurement_contracts]]. Zwei offene, nicht-blockierende Folgepunkte: DS-FOLLOWUP-08 (Fluggeschwindigkeit-Feintuning nach CI-Font-Anbindung, aktuell 1350ms) und die zwei Engine-Technical-Debt-Punkte `AP-prokrast-08-FOLLOWUP-A`/`-B` (No-op-Bootstrap-Entscheidung, `chartSettled`-Creation-Pfad-Lücke).

**Screen 3 gebaut als Kontinuitäts-Reveal, nicht als wörtlicher Drehbuch-Beat (AP-prokrast-10a–10d, 2026-07-07):** Das Drehbuch beschreibt für Screen 3 einen dreistufigen Text→Chart→KPI-Beat mit einem eigenen, seit AP-14 überholten Screen-3-Zitat („Im Rückblick ist es eine Linie...") — dieser Textteil ist im Drehbuch selbst NICHT als „abgelöst" markiert (anders als Screen 4s Beat 1/3), obwohl der real gebaute Screen-3-Text seit AP-14/B1-AP-16b ein anderer ist. Gebaut wurde stattdessen nach zwei Iterationen: (1) verworfener Zuschnitt „Text→Chart→KPI-Timing-Reveal" (fühlte sich wie Screen-Neustart an), (2) finale „Variante B++ — Kontinuitäts-Reveal": Chart+Ergebnislinie erscheinen beim Screen-3-Eintritt sofort/still (kein Chart-Hidden mehr), ein neues Screen-3-lokales Bridge-Element zeigt zunächst dieselbe „Station X von Y"-Zeile wie zuletzt auf Screen 2 (`formatStationProgress()`, aus `progressEl`-Formel extrahiert, keine Verschiebung), danach KPI+Disclaimer per 800ms-Fade. Die blaue Ergebnis-/Endlinie (`FwVerticalLinePlugin`, `verticalLine:'last'`) ist technisch nicht animierbar (siehe [[project_chartengine_anchormeasurement_contracts]] — Plugins nur bei erstem `new Chart()` lesbar, Plugin zeichnet zudem rein statisch) — Nutzerentscheidung: so lassen, keine Engine-Erweiterung. Vollständiges Herleitungsprotokoll inkl. der Architektur-Fork-Klärung (Stationszeile ist real ein Screen-2-Element): `docs/steering/patches/AP-prokrast-10a…10d_*_Ergebnis.md` (plus `10b_screen2-screen3-kontinuitaet_llm-review-kontext.md`, `10b_screen3-timing-reveal_..._Ergebnis.md` als forensisches Warnheader-Artefakt für den verworfenen Zuschnitt).

**Doku-/Spec-/QA-Sync auf AP-08/AP-10-Endstand (AP-prokrast-11a–11d, 2026-07-07):** `APP_SPEC.md`, Drehbuch und `QA_TEST_CASES.md` waren trotz gebautem und abgenommenem Screen-3-Kontinuitäts-Reveal (AP-10) und Card-to-Point (AP-08) noch nicht synchronisiert — beide Endstände standen dort weiterhin als „noch nicht gebaut" bzw. als der längst verworfene Text→Chart→KPI-Timing-Reveal. Sync-Analyse (AP-11a, GELB) fand die Lücke; Write-AP (AP-11b, GRÜN) synchronisierte alle drei Dokumente und führte dabei ein neues, vom Auftrag vorgegebenes Rahmungsmuster ein: `HISTORISCHER_STAND — NICHT AKTUELLER SOLLSTAND`-Blöcke (sechs Pflichtfelder, explizit an spätere LLM-Leser adressiert, nicht nur Menschen) um jeden verworfenen Planstand, getrennt von neuen `AKTUELLER_SOLLSTAND — GÜLTIGER ENDSTAND`-Blöcken. `APP_SPEC.md` erhielt einen neuen §16.1b (Kontinuitäts-Reveal, analog zum bestehenden §16.1a-Muster); Drehbuch erhielt einen „Amtlicher Hinweis"-Block analog zum Screen-4-Muster; `QA_TEST_CASES.md` erhielt TC-E06 (Bridge/Timing) und TC-E07 (Reduced Motion) neu sowie eine präzisierte TC-E04. Unabhängige Abschluss-QA (AP-11c, GRÜN) bestätigte alle Claims gegen reale Dateien und fand dabei einen weiteren, außerhalb des AP-11-Scopes liegenden Fund: die Implementierungs-Notiz zu RubikonSymbolMarkers im Drehbuch (Z.240, „Bau noch offen") ist stale, obwohl AP-07 sie längst fertiggestellt hat — als BACKLOG AP-prokrast-11-FOLLOWUP registriert, bewusst nicht repariert. A11y-/Reduced-Motion-Befund (AP-11d): kein Hinweis auf Verlust, da AP-11 keinen Code geändert hat; Reduced Motion wurde nur statisch/implizit geprüft, kein praktischer Browser-/Screenreader-Test. Vollständiges Protokoll: `docs/steering/patches/AP-prokrast-11a…11d_..._Ergebnis.md`.

**Wie anwenden:** Bei Arbeit an Screen 4 zuerst `APP_SPEC.md` §16.1a lesen (operative Spec); bei Arbeit an Screen 3 zuerst §16.1b lesen; `QA_TEST_CASES.md` Gruppe E (Screen 3) bzw. F (Screen 4) für Abnahmekriterien. In `APP_SPEC.md`/Drehbuch auf `HISTORISCHER_STAND`- vs. `AKTUELLER_SOLLSTAND`-Blöcke achten — nur Blöcke mit Status AKTIV/ABGENOMMEN/GÜLTIG sind gültiger Sollstand, HISTORISCHER_STAND-Blöcke dürfen nie als Implementierungsauftrag oder QA-Soll gelesen werden. Bei Arbeit an Screen 2 (Card-to-Point) oder Screen 3 (Kontinuitäts-Reveal) zuerst [[project_chartengine_anchormeasurement_contracts]] lesen — die Contracts existieren bereits, kein neuer Koordinaten-Kanal nötig, und die Plugin-Plattformgrenze (nur bei erstem `new Chart()` lesbar) gilt für jede Chart-Funktion, nicht nur `anchorMeasurement`. Bei Positionsänderungen an `rubikonText`/RubikonSymbolMarkers: `tools/rubikon-symbol-markers-diagnose.js` in der Browser-Konsole nutzen statt manuell zu schätzen. Alle 4 Screens sind jetzt gebaut UND dokumentiert-synchron — kein Teil der App ist mehr „noch ungebaut, Drehbuch ist alleinige Soll-Quelle" oder „noch nicht dokumentiert". Nächster Schritt: Commit-Umfang für AP-prokrast-11a–11d durch Masterfaden entscheiden; danach Wahl zwischen AP-prokrast-08-FOLLOWUP-A (No-op-Bootstrap), DS-012/DS-013 oder dem kleinen AP-prokrast-11-FOLLOWUP (Drehbuch-Korrektur) als nächster AP-Kandidat.

---
name: project-prokrastinations-preis-drehbuch
description: "prokrastinations-preis: Drehbuch war Soll-Quelle für Screen 4; seit AP-prokrast-04a (2026-07-03) ist APP_SPEC.md §16.1a die synchronisierte, operative Spec für den gebauten Rubikon-Endstand; QA_TEST_CASES.md seit AP-prokrast-05a-05e (2026-07-03) ebenfalls synchron inkl. CTA-Fokus-Testabdeckung; Beat-2-✅/❓-Symbolik seit AP-prokrast-06a-06d (2026-07-03/04) entschieden und seit AP-prokrast-07a-07d (2026-07-06) fertig gebaut, feinjustiert und GRÜN abgenommen (FwChartTextPlugin.js anchor:'lastPoint', BACKLOG AP-26 archiviert); Screen 2 Card-to-Point-Motion seit AP-prokrast-08a-08c (2026-07-06) gebaut, zweimal grundlegend korrigiert, unabhängig hart geprüft (GELB, kein Blocker); zwei offene Folgepunkte DS-FOLLOWUP-07/08 (Font-Neumessung) + AP-prokrast-08-FOLLOWUP-A/B (Engine-Technical-Debt); Drehbuch bleibt nur noch für Screen-3-Timing-Reveal amtlich"
metadata: 
  node_type: memory
  type: project
  originSessionId: a35e85df-d14e-477e-b4b3-0052c090219a
---

`Apps/prokrastinations-preis/drehbuch_prokrastinationspreis_app.md` (Stand 2026-06-25) ist das amtliche, freigegebene fachliche Zielbild für die App `prokrastinations-preis`. Für Screen 4 ist seit AP-prokrast-04a (2026-07-03) `APP_SPEC.md` §16.1a die synchronisierte, operative Spec-Quelle — sie beschreibt den tatsächlich gebauten, von Albert bestätigten stehenden Rubikon-Screen (kein Morph, kein C2, DOM-Overlay-Text), nicht mehr die ursprüngliche Drehbuch-Choreografie mit Achsenanimation. Für Screen 2 (Card-to-Point) und Screen 3 (Timing-Reveal) bleibt das Drehbuch weiterhin die alleinige Soll-Quelle, da diese Teile noch nicht gebaut sind.

**Warum wichtig:** Die Drehbuch-Datei ist im Git-Status untracked. Ein Claude, der nur `git log`/`git status` prüft, würde sie leicht übersehen oder ihre Autorität anzweifeln. Der untracked-Status ist ausdrücklich nur ein Commit-Hinweis, keine fachliche Unsicherheit. Für Screen 4 gilt jetzt zusätzlich: Drehbuch-Beat 1 (Achsenanimation) und Beat 3 (Text unter dem Chart) sind im Drehbuch selbst als „abgelöst" markiert, Beat 2 (✅/❓-Symbolik) explizit als „offen, nie entschieden" — nicht als abgelöst verwechseln.

**Kernbefund aus AP-prokrast-02c (Architektur-Kontrakt):** Der bestehende `xDisplayRange`/`displayRange`-Mechanismus (Chart-Engine, AP-14b) trägt die Rubikon-Zukunftsraum-Mechanik (Screen 4) bereits strukturell — keine Obergrenzenprüfung in `LineChartStrategy.transform()`, `FwVerticalLinePlugin` positioniert sich bereits am letzten echten Datenpunkt. Card-to-Point (Screen 2, Kartenschrumpf-Animation) hat dagegen keine bestehende Koordinaten-Schnittstelle — höchstes Architekturrisiko im gesamten Drehbuch, weiterhin ungebaut.

**`QA_TEST_CASES.md`-Sync abgeschlossen (AP-prokrast-05a–05e, 2026-07-03, BACKLOG AP-27 erledigt):** `Apps/prokrastinations-preis/QA_TEST_CASES.md` Gruppe F ist jetzt vollständig mit `APP_SPEC.md` §16.1a synchron. TC-F01 behandelt den Rubikon-Chart nicht mehr als Fehler (alte Formulierung „Kein Zukunftschart." entfernt). TC-F03/TC-F04 decken DOM-Haupttext, 800ms-Timing und — nach einem in AP-05b gefundenen und in AP-05c geschlossenen Nachputz — die CTA-Nicht-Erreichbarkeit während beider 800ms-Pausen (inkl. Reduced-Motion-Invarianz) testbar ab. Ausschließlich `QA_TEST_CASES.md` geändert, kein Code/Spec/Drehbuch.

**RubikonSymbolMarkers gebaut (AP-prokrast-07a, 2026-07-06):** `FwChartTextPlugin.js` additiv um `anchor:'lastPoint'` erweitert — bindet ✅/❓ an denselben Datenpunkt wie `FwVerticalLinePlugin` (`chart.getDatasetMeta(0)`, letzter Punkt), keine eigene/divergierende Zeitberechnung, kein DOM, keine A11y-Semantik. `app.js` `renderScreen4Chart()` ergänzt.

**CSS-Feinjustierung + Abschluss (AP-prokrast-07b–07d, 2026-07-06):** Albert fand beim manuellen Live-Server-Test zwei Layout-Defekte am DOM-Textblock `rubikonText` — Kollision mit „?" auf Screen S, fehlende gemeinsame Fluchtlinie zu „?" auf allen Breiten. Behoben über iterative DevTools-Messung ([[feedback_css_custom_properties_tuning]], Skript `tools/rubikon-symbol-markers-diagnose.js`) und eine neue M-Breakpoint-Stufe in `app.css` (481–1024px, zusätzlich zu den bestehenden S/L-Regeln, da eine gemeinsame Basisregel M und L nicht gleichzeitig treffen konnte). Unabhängige Abschluss-QA (AP-07b, GELB) fand M zunächst offen und den formalen DOM-/Accessibility-Tree-Check noch nicht dokumentiert — sowie den Gegenbefund, dass `CSS-KONVENTIONEN.md` nur für `screen.css` gilt, nicht für App-eigene Stylesheets. QA-Nachtrag (AP-07d, GRÜN): Albert nahm M unter dem aktuellen Fallback-Font-Stand bewusst ab (Feinjustierung erst nach CI-Font-Anbindung sinnvoll) und führte den DOM-/Accessibility-Tree-Check durch (kein DOM-Treffer, kein Accessibility-Tree-Eintrag, keine Live-Region-Aktualisierung) — TC-F05 damit für den aktuellen Font-Stand bestanden, Commit aus AP-07-Sicht freigegeben. **Wichtiger Fund (bleibt gültig):** DOM-Text und Canvas-„?" nutzen aktuell beide Browser-Default-`sans-serif`, nicht die offiziellen CI-Fonts ('Source Sans Pro'/'Archivo Black' sind im Theme längst per `@font-face` geladen, aber `--fw-font-base` ist nirgendwo definiert — Theme-Bridge-Lücke, BACKLOG DS-012/DS-013). Sobald Fonts angebunden werden, ändern sich Zeichenbreiten — komplette Neumessung nötig, als BACKLOG DS-FOLLOWUP-07 registriert. `app.js` darf laut `APP-INTERFACE.md` §4 keine Chart.js-Pixelinternals lesen, deshalb bleibt die Positionierung statisch in CSS statt live berechnet.

**Offene Punkte nach AP-prokrast-07 (BACKLOG AP-26 archiviert, Stand 2026-07-06):** Kein AP-07-Blocker mehr offen. Nachfolgepunkt DS-FOLLOWUP-07 (Rubikon-Neumessung nach CI-Font-Anbindung, hängt an DS-012/DS-013).

**Card-to-Point Screen 2 gebaut (AP-prokrast-08a–08c, 2026-07-06):** Damit ist auch der zweite der beiden im Drehbuch als architektonisch riskant markierten Bausteine gebaut. Analyse (AP-08a, GRÜN) bestätigte: kein Chart.js-Internals-Zugriff aus `app.js` nötig, neues eigenständiges `FwAnchorMeasurementPlugin.js` als Lösung. Implementierung (AP-08b, GELB) + Sequenzkorrektur (AP-08b2) + Chart-Settled-Gate (AP-08b3) + Architektur-Gate (AP-08b4a) + RenderMotion (AP-08b5) + harte Abschluss-QA (AP-08c, GELB) — volle Herleitung inkl. der neuen Engine-Contracts (`anchorMeasurement`, `chartSettled`, `renderMotion`) und der Chart.js-Plugin-Registrierungs-Plattformgrenze in [[project_chartengine_anchormeasurement_contracts]]. Zwei offene, nicht-blockierende Folgepunkte: DS-FOLLOWUP-08 (Fluggeschwindigkeit-Feintuning nach CI-Font-Anbindung, aktuell 1350ms) und die zwei Engine-Technical-Debt-Punkte `AP-prokrast-08-FOLLOWUP-A`/`-B` (No-op-Bootstrap-Entscheidung, `chartSettled`-Creation-Pfad-Lücke).

**Wie anwenden:** Bei Arbeit an Screen 4 zuerst `APP_SPEC.md` §16.1a lesen (operative Spec); `QA_TEST_CASES.md` Gruppe F für Abnahmekriterien. Bei Arbeit an Screen 2 (Card-to-Point) zuerst [[project_chartengine_anchormeasurement_contracts]] lesen — die Contracts existieren bereits, kein neuer Koordinaten-Kanal nötig. Bei Arbeit an Screen 3 (Timing-Reveal) bleibt das Drehbuch die alleinige Soll-Quelle, noch ungebaut. Bei Positionsänderungen an `rubikonText`/RubikonSymbolMarkers: `tools/rubikon-symbol-markers-diagnose.js` in der Browser-Konsole nutzen statt manuell zu schätzen. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-01…08c_*_Ergebnis.md` (plus `08b2_llm-review-kontext.md`). Nächster Schritt: Commit-Freigabe durch Albert; danach Masterfaden-Entscheidung über die zwei Engine-Folgepunkte, dann Screen-3-Timing-Reveal oder DS-012/DS-013 als nächster Haupt-AP-Kandidat.

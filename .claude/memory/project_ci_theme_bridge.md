---
name: project-ci-theme-bridge
description: "CI-/Theme-Bridge — Engine/Theme-Seite umgesetzt+committed (1357b46), Spec-Wortlaut nachgeführt (kdr14, committed 21e87a2), App-Pilot-Farbmigration app.css umgesetzt+committed (AP-17, 6ea9704); QA-Doku + unabhängiges Kettenreview abgeschlossen (17b/17c/18/19, committed 8a4163a); Font-Bridge umgesetzt (AP-17-FONT-CODE-A/B, uncommitted) — FwTheme.init() liest --font-display/--font-body aus tokens.css, Canvas+HTML-UI token-gespeist, Spec-Status auf implementiert (Mechanismus+Pilot) gehoben; tools/ci-token-check.js um universelles Ist/Soll-Set-Audit fwCiAudit() erweitert (committed ab95cfd), fand 3 CI-Bugs — BTNFONT+H3LEAK durch TESTENV-1g behoben (Albert-verifiziert, uncommitted), BORDER bewusst bis Tailwind zurückgestellt; TESTENV-1-Initiative vollständig abgeschlossen. NEU (2026-07-12): AP-tailwind-01-Befund (0 Tailwind-Utility-Klassen im App-Pool trotz kanonischer CI-POOL-Entscheidung) → Fable-Entscheidungsrunde → TAILWIND-APP-BAUKASTEN_KONZEPT_V0-1.md als verbindlicher Design-Vertrag freigegeben (Albert). NEU (2026-07-13): AP-tailwind-02a/02b — Play-CDN-Vertrag (Tailwind v4) repo-weit entschieden und gehärtet. AP-tailwind-02 Slice 1 (Shell/States) + Slice 2 (KPI) umgesetzt und browserabgenommen — inkl. AP-tailwind-02c (Loading-Taxonomie-Vorarbeit), 02d (Play-CDN-Manifest + Error-Taxonomie, Slice 1 zunächst ROT), 02e (fehlende Theme-Bridge nachgerüstet, danach GRÜN). tools/ci-token-check.js um fwKpiCardCheck() erweitert. Nächster Schritt: AP-tailwind-02 Slice 3 — weitere Baukasten-Primitiven (Detail-Prompt offen)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 74f7b4cf-2bbe-4649-bb8f-ad725d807d28
---

AP-prokrast-14a–14c (2026-07-07) hat die CI-/Theme-Bridge zwischen `screen.css` (einzige CI-Farb-/Font-Quelle) und Apps analysiert. Kernbefund: A-04 (`docs/App-Fabrik/01_DECISION_LOG.md`, 🟢 ENTSCHIEDEN) fordert, dass Apps dieselbe CSS-Variablen-Theme-Bridge wie die Chart-Engine nutzen (`FwTheme.js`-Muster) — im einzigen real gebauten App-Code (`Apps/prokrastinations-preis/app.css`) ist das nicht umgesetzt. `app.css` nutzt einen eigenen `--fw-*`-Namensraum, dessen 11 CI-semantische Tokens (Farbe/Font/Spacing) repo-weit nirgendwo definiert sind (deterministisch bestätigt, AP-14aR). 6 der 17 `--fw-*`-Tokens sind dagegen funktionierende, bewusst lokale App-Mechanik (Timing/Positionierung) — keine Bridge-Lücke, dürfen nicht mitmigriert werden.

Zusätzlicher Befund (AP-14b): `screen.css` hat nur Marken-Tokens (petrol/blau/purpur/gelb), aber keine Rollen-Tokens (primary/surface/error-*), die `app.css` braucht. Eine reine `--fw-*`→`--color-*`-Umbenennung würde daher ins Leere laufen — es fehlen 3 echte neue Farbentscheidungen (Primary-Zuordnung, Error-Farbfamilie, ggf. Surface), bevor migriert werden kann.

Empfohlener Zielkontrakt: Variante C (Hybrid) — CI-semantische Tokens zentral über `--color-*`, App-Mechanik bleibt app-lokal unter dokumentiertem `--fw-*`-Namensraum. Variante B (Alias-Layer `--fw-*` in `screen.css`) wurde verworfen, da sie der bestehenden `docs/steering/design/CSS-KONVENTIONEN.md`-Regel „`fw-*` ist reserviert für die Chart-Engine, nicht in `screen.css`" widerspricht.

Drei weitere App-Ordner (`regulatorik-dashboard`, `rollierende-sparplaene`, `weltkarte-etf-indizes`) enthalten nicht ins App-Fabrik-Muster integrierte Standalone-HTML-Prototypen mit eigenem Farb-/Font-System — kein AP-15-Scope, aber vorhanden und potenzielles Drift-Risiko für spätere App-Autoren, die sie fälschlich als Vorlage kopieren könnten.

**UMSETZUNG (AP-prokrast-15a–16c, 2026-07-09, DS-015 damit erledigt):** Die Farbrollen wurden entschieden und die Engine-/Theme-Seite umgesetzt. `Theme/assets/css/tokens.css` NEU ist die Single Source of Truth (volle Tailwind-kompatible Leitern 50–900 Petrol/Gelb/Purpur, Seeds gepinnt, am 15c-Board abgenommen; Rollen primary=Petrol/link=Blau/surface=bg-faint/error-Set zentralisiert). `screen.css` importiert `tokens.css` via `@import`, `:root`-Farbblock entfernt. `FwTheme.init()` bridged tokens.css; die drei Strategien bekommen das eine init()'te Theme per Composition Root durchgereicht (AP-16c, Constructor Injection mit Graceful Default) — Chart-Farben laufen live über die CSS-Kaskade, Null-Delta belegt (19/19 byte-identisch). Verbindliche Regeln: `docs/steering/design/CI-POOL-ROLLENKONTRAKT.md`. Kaskaden-Harness: `Theme/chart-tests/AP-16-abnahme.html` (von Albert verifiziert).

**UMSETZUNG committed:** Die gesamte Kette AP-prokrast-15a–16c ist als Commit `1357b46` im Repo (git log verifiziert, 2026-07-09).

**Spec-Wortlaut nachgeführt (AP-prokrast-kdr14, 2026-07-09):** `docs/spec/ARCHITECTURE STRATEGY PAPER VX.md` KDR 14 + Datei-Matrix-Theme-Zelle sprachen bis dahin noch von „aktuell hardcoded; geplant" — auf den real umgesetzten Composition-Root-Stand synchronisiert (kein Code geändert). Dabei entdeckt: Kontrakt (`CI-POOL-ROLLENKONTRAKT.md` §8/P15) nennt 20 gelesene `--color-*`-Properties, die AP-16c-Null-Delta-QA maß aber 19 — Widerspruch nicht aufgelöst, sondern als **AP-prokrast-16-FOLLOWUP-B** registriert (Spec nennt die Zahl jetzt nicht mehr konkret, nur noch qualitativ „Null-Delta-QA, AP-16c").

**App-Pilot-Farbmigration umgesetzt (AP-prokrast-17, 2026-07-09):** `Apps/prokrastinations-preis/app.css` — 24 CI-Farb-Referenzen `--fw-color-*` → Kontrakt-Rollen-/Farbtokens `--color-*` (8 Tokens `text/bg/primary/muted→text-muted/surface/error-text/-border/-bg`, Fallbacks = tokens.css-FINAL); `tokens.css` als `<link>` in `app.test.html` gebunden (Fork-1 Variante A, nur Dev-Harness — Ghost/Prod bleibt T1). Bewusst NICHT migriert: `--fw-font-base` (Kontrakt §9 Font-Ausklammerung + Rubikon-Kopplung DS-FOLLOWUP-07), `--fw-space-*` (§7.6, T1), alle Mechanik-`--fw-*` (§2/§4), `app.js`. Sichtbare Änderung: `primary` Blau→Petrol (§1.1-Anomalie-Korrektur); `muted/text/surface` driften klein (ΔE ≤7). Albert-Live-Farbabnahme + Konsolen-ΔE-Gegenprobe bestätigt (5/5 Match, tokens.css greift live). Generisches QA-Tool `tools/ci-token-check.js`. Uncommitted. Ergebnisprotokoll: `docs/steering/patches/AP-prokrast-17_pilot-migration-prokrastinations-preis_Ergebnis.md`.

**App-Pilot-Migration committed:** AP-prokrast-17 ist als Commit `6ea9704` im Repo (git log verifiziert, 2026-07-10).

**QA-Doku + unabhängiges Kettenreview + Abschluss-Korrektur (AP-prokrast-17b–19, 2026-07-10):** `QA_TEST_CASES.md` Gruppe N (TC-N01–N09) für die AP-17-Farbmigration ergänzt (17b) + Übersichtstabelle mit Body synchronisiert (17c, vorbestehende Lücke Gruppe P nachgetragen). Unabhängiges Claims-vs-Files-Review der Gesamtkette AP-15a–17c (18, Opus, GELB): alle 8 load-bearing Substanz-Behauptungen bestätigt (GRÜN). **Property-Zahl-Widerspruch 19 vs. 20 aufgelöst:** `FwTheme.init()` liest 19 `--color-*`-Properties; das `colors`-Objekt hat 20 Keys, der zwanzigste (`linesDark`) wird aus `textSec` abgeleitet, nicht aus CSS gelesen — die 16c-QA maß richtig (19), der Kontrakt hatte Farb-Tokens gezählt und sie fälschlich „gelesene Properties" genannt. Kontrakt-Wortlaut korrigiert (19, §9/P15). Fünf weitere Doku-Falschaussagen gefunden und korrigiert: `screen.css` fälschlich „Engine-intern" genannt (real das produktive Ghost-Stylesheet — `tokens.css` ist darüber transitiv Teil der Produktionskaskade, AP-16-Design, kein Fehler); `QA_TEST_CASES.md` TC-N05 prüfte strukturell nie auslösbar (kein `.hbs` im Repo) und wurde auf die reale Invariante (App-Card-Kaskade) umformuliert; `NAVIGATION.md`-Hex-Regel war zu absolut (zwei reale Neutral-Hexe in `screen.css`); Spec nannte `FwTheme`-Renderer statt `FwRenderer`; `screen.css`-Kopfkommentar war doppelt veraltet. Kein Code-/Engine-Diff über die gesamte Vierer-Kette.

**Font-Anamnese + Spec-Parität (AP-prokrast-17-FONT-ANAMNESE + AP-prokrast-17-FONT-SPEC-PARITÄT, 2026-07-10, kein BACKLOG-Punkt, direkt archiviert):** Breite Font-Einbindungs-Inventur (26 Dateien, Python-deterministisch) fand die reale Bruchstelle für die App-Font-Kette: **kein fehlender Bridge, sondern ein Namens-Mismatch** — `app.css` referenziert `--fw-font-base`, das SSoT-Token heißt `--font-display`/`--font-body` (`tokens.css`); Ersteres ist repoweit nirgendwo definiert, Fallback `sans-serif` greift daher immer. Zusatzfund: die Chart-Engine hat eine **zweite, eigenständige, nie CSS-gebrückte Font-Quelle** — `FwTheme.js` `this.fonts` ist ein reiner Konstruktor-Hardcode, `init()` liest nachweislich nur Farben (der Klassenkommentar behauptet fälschlich auch Fonts). `styles.css`/`stylesheet.css` beide tot bestätigt (nur DEPRECATED-Kommentar, keine reale Referenz mehr). Darauf aufbauend wurde die **Font-Spec auf KDR-14-Zielmechanismus-Parität zu Farben gehoben** (Alberts Entscheidung 2026-07-10, „Spec schlägt Code"): `ARCHITECTURE STRATEGY PAPER VX.md` KDR 14 neuer Punkt 5 (Font-Äquivalent zum Farb-Mechanismus, Status damals „Code-Umsetzung offen"), `TECH-SPEC Theme-Integration Chart-Engine.md` §5.4 umgeschrieben (Ist-Zustand lokal/DSGVO bereits real seit AP-16, veraltete Google-Fonts-CDN-Empfehlung entfernt), `CI-POOL-ROLLENKONTRAKT.md` §9 neuer Font-Bridge-Zielmechanismus-Absatz. Farb-Staleness TECH-SPEC Z.286 („CSS-Bridge geplant, noch nicht implementiert") bewusst nur geflaggt, nicht gefixt. Kein Code-Diff über beide APs. Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17-FONT-ANAMNESE_font-einbindung-breite-inventur_Ergebnis.md`, `..._17-FONT-SPEC-PARITAET_Ergebnis.md`.

**Font-Bridge umgesetzt (AP-prokrast-17-FONT-CODE-A/B + SPEC-HEBUNG, 2026-07-10/11, kein BACKLOG-Punkt für CODE-A/B, direkt archiviert; FOLLOWUP-FONT damit erledigt):** `FwTheme.init()` liest jetzt real `--font-display`/`--font-body` aus `tokens.css` (Constructor-Hardcode bleibt Fallback für Test-HTMLs ohne Tokens) — exakt derselbe Mechanismus wie bei Farben. **Pfad A (Canvas, 6 Engine-Dateien: `FwTheme.js`, `FwLayoutRules.js`, `FwSmartXAxis.js`, `FwSmartYAxis.js`, `FwSmartTooltips.js`, `BarChartStrategy.js`):** Achsen-Ticks/Tooltip/Donut-Center ziehen die Schrift über die injizierte init()'te Theme-Instanz; unabhängig reviewt (Opus, frische Instanz, GRÜN), Null-Delta belegt. **Pfad B (HTML-UI, `Apps/prokrastinations-preis/app.css`):** `--fw-font-base` (nirgendwo definiert, Fallback `sans-serif`) → `--font-body`; App-Fließtext/-Headlines (H-A: body-only, kein `--font-display`), A11y-Tabelle-im-Piloten und App-Meldungen ziehen automatisch mit. Engine-injizierte HTML-Flächen (Legende, Range-Buttons, BAN, Popover, Fehlermeldung) waren durch Pfad A bereits token-gespeist — verify-only, kein Diff. Albert-browserverifiziert am Piloten (`app.test.html`): alle 5 HTML-UI-Flächen `document.fonts.check` ✅, Font real geladen. **Tool-Aufwertung:** `tools/ci-token-check.js` um `fwFontCheck()` erweitert (Font-Token- + Element-Prüfung, inkl. Härtungsfix gegen Fehlalarm bei undefinierten Tokens auf Legacy-Seiten). **Spec-Status gehoben (AP-prokrast-17-FONT-SPEC-HEBUNG, 2026-07-11):** KDR 14 P5, TECH-SPEC §5.4, CI-POOL §9 jetzt „implementiert (Mechanismus + Pilot)" — ausdrücklich **nicht** App-Pool. **Abweichung von der ursprünglichen BACKLOG-Kopplungsauflage:** `AP-prokrast-17-FOLLOWUP-FONT` verlangte, die Rubikon-Nachmessung MÜSSE gemeinsam mit/vor der Migration laufen; real geschah die Migration zuerst, die Nachmessung wurde separat offen gelassen — von Albert im CODE-B-Auftrag explizit autorisiert (Nicht-Ziel). `DS-FOLLOWUP-07` bleibt daher offen, jetzt sachlich fällig (nicht mehr nur vorsorglich). Nebenprodukt: `docs/steering/patches/TESTENV-1_harness-inventar_moderne-vs-legacy.md` (Bestandsaufnahme moderne vs. Legacy-Test-Harnesse, Vorarbeit für TESTENV-1). Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-17-FONT-CODE-A-CANVAS_Ergebnis.md`, `..._REVIEW_Ergebnis.md`, `..._CODE-B-HTMLUI_Ergebnis.md`, `..._SPEC-HEBUNG_Ergebnis.md`.

**Tool-Aufwertung `fwCiAudit()` (2026-07-11, im Zuge von `TESTENV-1eB`/`-1f`, committed `ab95cfd`):** `tools/ci-token-check.js` wurde auf Alberts Wunsch von einer kuratierten Selektor-Liste (fehleranfällig: prüft ein Feature, das auf der geöffneten Seite gar nicht existiert) auf ein primitives, universelles Ist/Soll-Set-Audit umgebaut. `fwCiAudit()` diskoviert die erlaubte Farb-/Font-Menge live aus `tokens.css` (`:root`, `--color-*`/`--font-*` per CSSOM, kein Hardcode) und prüft jeden tatsächlich benutzten, eindeutigen Wert innerhalb der `.kg-card`-Bereiche der geöffneten Seite dagegen — reine Mengen-Zugehörigkeit, kein Design-/Layout-Urteil. `auditCanvasCharts()` liest zusätzlich über die Chart.js-Registry (`Chart.getChart`, Chart.js v4.5.0) die echten Serien-/Tooltip-/Plugin-Farben aus jedem `<canvas>`. Über drei reale Testrunden (App-Testseite + 2 Engine-Testseiten) dreimal aus echten Funden präzisiert: Tooltip-Farben nur werten wenn `enabled !== false` (Pie-Charts mit `infoSystem:'CENTER_TEXT'` haben ein totes, nie gemaltes Tooltip-Plugin mit toten Chart.js-Default-Farben); `.kg-card` selbst nicht mitwerten (nur Testseiten-Chrome, keine App-/Chart-Farbe — `TEST_PAGE_STANDARD.md` §6); `color`/`font-family` bei `<input type="range/checkbox/radio">` nicht werten (unsichtbarer Browser-Default), stattdessen `accent-color` (die tatsächlich sichtbare Reglerfarbe). Kopfkommentar um eine „ERWEITERUNG"-Anleitung für künftige LLMs ergänzt (3 konkrete Erweiterungspunkte: neues Chart-Engine-Plugin mit Farboption, neuer textloser Input-Typ, neues `enabled:false`-Muster; Pflicht-Reihenfolge davor: erst Quellcode belegen, dann erweitern, nie einen Fund stillschweigend rausfiltern). Fand dabei 3 CI-Bugs (`.fw-app__btn` Arial-Font, `h3.fw-chart-title` CSS-Leck aus TESTENV-1eB-Migration, `.financial-chart-module` `#ddd`-Rahmen). Alle Änderungen ausschließlich in `tools/ci-token-check.js`, kein Produktionscode. Patch-Quittungen: `docs/steering/patches/PATCH-CI-AUDIT-TOOL*_2026-07-11.md` (4 Stück).

**`TESTENV-1g` (2026-07-11, GRÜN) — die beiden ersten CI-Bugs behoben, TESTENV-1-Initiative damit vollständig abgeschlossen:** Button-Font: `app.css` `.fw-app__btn` um `font-family: inherit;` ergänzt (1 Zeile), Albert-verifiziert. H3-Leak: Python-Scan fand den identischen bare-`h3`-Block entgegen der ursprünglichen Vermutung nicht in 1, sondern 4 Dateien (`tooltip.test.html`, `cadence-density.test.html`, `irregular-bar.test.html`, `irregular-line.test.html`) — zunächst an der im Auftrag gesetzten 3-Dateien-Grenze gestoppt und gemeldet, von Albert im selben AP auf 4 erweitert. Fix: bestehender Container `.fw-test-case` per Kombinator-Wechsel (Nachkomme → Direkt-Kind, `h3` → `.fw-test-case > h3`) verengt — trennt die gewollten Demo-Caption-Überschriften vom zwei Ebenen tiefer verschachtelten `.fw-test-expected h3` und vom Engine-erzeugten `h3.fw-chart-title` (innerhalb `.kg-card`, kein Sektions-Direktkind), keine neue Wrapperstruktur, kein `!important`. Zusätzlich `tests/shared/test-page.css` `.fw-test-expected h3` um `color: var(--color-text, #272727)` ergänzt. Beide Fixes automatisiert verifiziert (`check-test-pages.py` GRÜN, Python-Nachweis auf Selektor-Statement-Ebene) und von Albert browserseitig bestätigt, inklusive der ursprünglich aus `TESTENV-1f` §5 offenen Browser-Stichprobe. `TESTENV-1-FOLLOWUP-BORDER` bleibt eigenständig offen (bewusst bis Tailwind zurückgestellt). Ergebnisprotokoll: `docs/steering/patches/TESTENV-1g_ci-regressionsfix_Ergebnis.md`. Uncommitted, Commit-Freigabe liegt bei Albert.

**Tailwind-App-Baukasten V0.1 freigegeben (AP-tailwind-01 → Fable-Runde, 2026-07-12):** `AP-tailwind-01` (Befund, GELB) fand: Tailwind war seit dem CI-POOL-Kontrakt (§2–§7.6) kanonisch als Struktur-Fundament entschieden, aber im App-Pool faktisch nicht umgesetzt — `prokrastinations-preis` (einzige produktive App) hatte 0 Tailwind-Utility-Klassen, 3 Standalone-Prototypen (`regulatorik-dashboard`, `rollierende-sparplaene`, `weltkarte-etf-indizes`) ein komplett CI-fremdes eigenes Design-Token-System, 13 von 15 Chart-Engine-Testseiten einen hartcodierten Legacy-Border statt CI-Token. Darauf aufbauend hat eine Fable-Entscheidungsrunde 16 offene Fragen (D-01–D-16) entschieden und `docs/steering/design/TAILWIND-APP-BAUKASTEN_KONZEPT_V0-1.md` (+ Visual Board + Mockups) als neuen, von Albert freigegebenen, verbindlichen Design-Vertrag für alle 25+ Apps etabliert: `fw-app` als Hybrid-Scope-Anker (Klasse + Tailwind-Utilities), Section/Panel/Card-Taxonomie, Ein-Container-Vertrag am App↔Chart-Übergang (Chart-Chrome folgt der realen `FwRenderer`-Slot-Reihenfolge Titel→BAN→Toolbar→Legende→Canvas), Donut- statt Torten-Ist-Bild, 4 Button-Varianten, `sm/md/lg` für generische UI (App-Mechanik-Sonderbreakpoints wie die Rubikon-Positionierung bleiben app-lokal), Tailwind-Defaults für Spacing/Radius + nur die bestehenden `shadow-soft`/`-hover`-Zusatzstufen, JS-Klassen-Konstanten statt `@apply` (Literalregel — verhindert dynamisch zusammengesetzte, beim späteren lokalen Build purge-gefährdete Klassennamen), `--fw-space-*` wird ersatzlos abgelöst. Parallel wurde die alte Design-Ablage konsolidiert: Jahrgang 2026-05 (`docs/design-system`, u. a. verbotene Farbnamen `-tint/-20/-80` mit anderer Bedeutung als in `tokens.css`) nach `Archiv/design-system-2026-05/` verschoben, `docs/design-system/` ist jetzt nur noch Statuskarte + Icons-Spec. Umsetzung an der Pilot-App ist `AP-tailwind-02` (Sonnet, Handover: `docs/steering/handovers/SONNET-PROMPT_AP-tailwind-02_pilotmigration_V1.md`). Ergebnisprotokoll: `docs/steering/patches/AP-tailwind-01_befund-und-forschung_Ergebnis.md`. Uncommitted — Commit-Message liegt vor, Freigabe liegt bei Albert.

**Play-CDN-Vertrag entschieden und gehärtet (AP-tailwind-02a/02b, 2026-07-13, uncommitted):** Vor der eigentlichen Pilotmigration musste die im Konzept-Kopf als „Arbeitsannahme" markierte Tailwind-Laufzeitfrage (CDN vs. Ghost-Build) geklärt werden — `AP-tailwind-02a` hat Tailwind v4 Play-CDN (`https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4`) als Phasentrennung (vorproduktive Testphase = CDN, Produktion nach Ghost-Integration = lokaler, bereinigter/minimierter Build T1) repo-weit in Decision Log A-04, Konzeptkopf, `TEST_PAGE_STANDARD.md` §10/§12/§14, App-Testseiten-Template, `Apps/prokrastinations-preis/app.test.html`, Checker (`tools/check-test-pages.py`, neue `validate_tailwind_cdn()`) und Pilotprompt verankert. Dabei entdeckt: der bloße CDN-Tag-Tausch (v3→v4) hätte VISUAL-BOARD/MOCKUPS unbrauchbar gemacht, weil Tailwind v4 kein globales `tailwind.config`-Objekt mehr liest — auf `@theme`-CSS-Syntax mit identischen, bereits freigegebenen Werten umgestellt. `AP-tailwind-02b` (Nachputz) hat den Checker auf bytegenauen URL-Vergleich gehärtet (Query/Fragment brechen die Kanonizität, `type="module"` case-insensitiv gesperrt) und eine veraltete BACKLOG-Klausel korrigiert, nachdem geprüft wurde, dass `session-start.ps1` AP-IDs nur aus dem 🟡-Aktiv-Abschnitt liest (die Zeile lag im Offen-Abschnitt, Änderung war hook-unsichtbar). `fw-app-template.html` bewusst nicht auf v4 migriert — enthält die laut Konzept §2.1 verbotenen Alt-Namen (`-80/-20/-30/-tint`), historisch relativ zum Baukasten-Vertrag. Ergebnisprotokoll: `docs/steering/patches/AP-tailwind-02a_play-cdn-vertrag-repo-sync_Ergebnis.md` (inkl. Nachtrag AP-tailwind-02b).

**Play-CDN-Migration Slice 1+2 abgeschlossen (AP-tailwind-02c/Slice 1/02d/02e/Slice 2, 2026-07-13, uncommitted):** Erster echter Utility-Migrationsschritt der Pilotmigration `prokrastinations-preis` nach dem in AP-tailwind-01 freigegebenen Baukasten-Vertrag. Slice 1 (Shell/States: `.fw-app`-Basis + Loading/Empty/Error) migrierte von reinem `app.css` auf Tailwind-v4-Klassenkonstanten (`FW_SHELL_CLASS` u. a.) — erste Browser-Abnahme ROT: Play-CDN generiert CSS für zur Laufzeit per JS gesetzte Klassen nicht zuverlässig (Manifest-Lücke) UND registriert die CI-Tokens selbst nicht als Tailwind-Utilities (fehlende Theme-Bridge) — zwei unabhängige Ursachen. **AP-tailwind-02d** löste die erste: Play-CDN-Manifest (`<style type="text/tailwindcss">` + `@source inline(...)`, mengenidentisch zu den zur Laufzeit gesetzten `app.js`-Klassen, `tools/check-test-pages.py` prüft das jetzt deterministisch und generisch — kein zweiter hartkodierter Klassenkatalog) + Error-Taxonomie-Korrektur (K/L/R sind fachlich Error, nicht Empty — `APP-INTERFACE.md` §8 präzisiert). **AP-tailwind-02e** löste die zweite: wertfreie `@theme inline`-Bridge (Tailwind-v4-CSS-Syntax, kein `theme.extend`-Objekt), die jeden benötigten CI-Token 1:1 auf sich selbst abbildet, kanonisch in `docs/testing/templates/app.test.template.html`, bytegleich in jeder App-Testseite (Checker vergleicht das per Klammertiefen-Extraktion). Danach Slice 1 vollständig GRÜN (Alberts Browser-Abnahme, 2026-07-13). **Slice 2 (KPI)** folgte direkt und lief ohne weitere Theme-Bridge-Überraschung durch — die drei KPI-Karten auf Screen 3 migriert (Baukasten §6.3-Rezept), inkl. Reduced-Motion-Nachweis. `tools/ci-token-check.js` (bereits bestehendes generisches CI-Audit-Tool, s. o.) um `fwKpiCardCheck()` erweitert — vierte, eigenständige Funktion neben `fwTokenCheck`/`fwFontCheck`/`fwCiAudit`, deren Farbe+Font-Scope bewusst unangetastet blieb. Ergebnisprotokolle: `docs/steering/patches/AP-tailwind-02_slice-1_shell-states_Ergebnis.md`, `..._02d_runtime-manifest_error-taxonomie_Ergebnis.md`, `..._02e_play-cdn-theme-bridge_Ergebnis.md`, `..._02_slice-2_kpi_Ergebnis.md`. Follow-up außerhalb des Scopes: `Apps/prokrastinations-preis/APP_SPEC.md` §12 State-Modell-Tabelle beschreibt die Empty-Zeile noch mit der jetzt taxonomisch überholten Bedingung. Nächster Schritt: AP-tailwind-02 Slice 3 (weitere Baukasten-Primitiven, Detail-Prompt offen).

**Noch offen:** (1) **CI-Fonts** umgesetzt für Mechanismus + Pilot (AP-prokrast-17-FONT-CODE-A/B, Spec gehoben) — offen bleibt der **App-Pool-Rollout** (auf die übrigen ~24 Apps) und die **Rubikon-Nachmessung** S/M/L (`RubikonSymbolMarkers`, `tools/rubikon-symbol-markers-diagnose.js`), jetzt sachlich fällig statt nur vorsorglich (DS-FOLLOWUP-07). (2) `--fw-space-*`→Tailwind + Ghost-`tokens.css`-Anbindung (T1) — inklusive der zwei untokenisierten Neutral-Hexe in `screen.css` (`#f9fafb`/`#f3f4f6`, AP-18-Fund) und `TESTENV-1-FOLLOWUP-BORDER` (`.financial-chart-module` `#ddd`-Rahmen). (3) Standalone-Prototypen-Divergenz bleibt bis T1 (Tailwind-Produktionsbuild). (4) Abnahme-Harness `Theme/chart-tests/` bleibt gitignored/unversioniert (AP-18-Fund) — unabhängig von `TESTENV-1`, das mit `TESTENV-1g` vollständig abgeschlossen ist. (5) Offene Live-Tests TC-N04 (Error-States)/TC-N08 (S/M/L-Matrix) — BACKLOG `AP-prokrast-17b-QA`. (6) TECH-SPEC Z.286 Farb-Staleness — nur geflaggt, evtl. separater Minifix.

Ergebnisprotokolle: `docs/steering/patches/AP-prokrast-14a…14c_*`, `..._15a_*`, `..._15b_*`, `..._15c_*`, `..._16_theme-migration_Ergebnis.md`, `..._16b_bridge-konsolidierung-kaskaden-harness_Ergebnis.md`, `..._16c_theme-durchleitung_Ergebnis.md`, `..._kdr14_spec-wortlaut-nachfuehrung_Ergebnis.md`, `..._17b_qa-testcases-ci-farbtoken_Ergebnis.md`, `..._17c_qa-uebersicht-gruppen-sync_Ergebnis.md`, `..._18_claims-vs-files-review-gesamtkette_Ergebnis.md`, `..._19_abschluss-korrektur-doku-falschaussagen_Ergebnis.md`, `..._17-FONT-ANAMNESE_font-einbindung-breite-inventur_Ergebnis.md`, `..._17-FONT-SPEC-PARITAET_Ergebnis.md`, `..._17-FONT-CODE-A-CANVAS_Ergebnis.md`, `..._17-FONT-CODE-A-REVIEW_Ergebnis.md`, `..._17-FONT-CODE-B-HTMLUI_Ergebnis.md`, `..._17-FONT-SPEC-HEBUNG_Ergebnis.md`, `TESTENV-1_harness-inventar_moderne-vs-legacy.md`.

Verwandte Memories: [[project_app_fabrik_struktur]], [[project_prokrastinations_preis_drehbuch]], [[project_chartengine_anchormeasurement_contracts]].
